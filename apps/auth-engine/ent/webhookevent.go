// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/webhookendpoint"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/webhookevent"
)

// WebhookEvent is the model entity for the WebhookEvent schema.
type WebhookEvent struct {
	config `json:"-"`
	// ID of the ent.
	// Unique Webhook Event ID identifier (e.g. whev_1a2b3c)
	ID string `json:"id,omitempty"`
	// Target Webhook Endpoint ID
	WebhookEndpointID string `json:"webhook_endpoint_id,omitempty"`
	// Event type string (e.g. user.created, session.revoked)
	EventType string `json:"event_type,omitempty"`
	// Dispatched JSON payload
	Payload map[string]interface{} `json:"payload,omitempty"`
	// HTTP status code received from target server (e.g. 200, 500)
	StatusCode int `json:"status_code,omitempty"`
	// Snippet of HTTP response body returned by target server for developer debugging
	ResponseBody string `json:"response_body,omitempty"`
	// Network connection or execution error traceback message
	ErrorMessage string `json:"error_message,omitempty"`
	// Dispatch status: success or failed
	Status webhookevent.Status `json:"status,omitempty"`
	// Dispatch timestamp
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the WebhookEventQuery when eager-loading is set.
	Edges        WebhookEventEdges `json:"edges"`
	selectValues sql.SelectValues
}

// WebhookEventEdges holds the relations/edges for other nodes in the graph.
type WebhookEventEdges struct {
	// WebhookEndpoint holds the value of the webhook_endpoint edge.
	WebhookEndpoint *WebhookEndpoint `json:"webhook_endpoint,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// WebhookEndpointOrErr returns the WebhookEndpoint value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e WebhookEventEdges) WebhookEndpointOrErr() (*WebhookEndpoint, error) {
	if e.WebhookEndpoint != nil {
		return e.WebhookEndpoint, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: webhookendpoint.Label}
	}
	return nil, &NotLoadedError{edge: "webhook_endpoint"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*WebhookEvent) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case webhookevent.FieldPayload:
			values[i] = new([]byte)
		case webhookevent.FieldStatusCode:
			values[i] = new(sql.NullInt64)
		case webhookevent.FieldID, webhookevent.FieldWebhookEndpointID, webhookevent.FieldEventType, webhookevent.FieldResponseBody, webhookevent.FieldErrorMessage, webhookevent.FieldStatus:
			values[i] = new(sql.NullString)
		case webhookevent.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the WebhookEvent fields.
func (we *WebhookEvent) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case webhookevent.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				we.ID = value.String
			}
		case webhookevent.FieldWebhookEndpointID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field webhook_endpoint_id", values[i])
			} else if value.Valid {
				we.WebhookEndpointID = value.String
			}
		case webhookevent.FieldEventType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field event_type", values[i])
			} else if value.Valid {
				we.EventType = value.String
			}
		case webhookevent.FieldPayload:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field payload", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &we.Payload); err != nil {
					return fmt.Errorf("unmarshal field payload: %w", err)
				}
			}
		case webhookevent.FieldStatusCode:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field status_code", values[i])
			} else if value.Valid {
				we.StatusCode = int(value.Int64)
			}
		case webhookevent.FieldResponseBody:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field response_body", values[i])
			} else if value.Valid {
				we.ResponseBody = value.String
			}
		case webhookevent.FieldErrorMessage:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field error_message", values[i])
			} else if value.Valid {
				we.ErrorMessage = value.String
			}
		case webhookevent.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				we.Status = webhookevent.Status(value.String)
			}
		case webhookevent.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				we.CreatedAt = value.Time
			}
		default:
			we.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the WebhookEvent.
// This includes values selected through modifiers, order, etc.
func (we *WebhookEvent) Value(name string) (ent.Value, error) {
	return we.selectValues.Get(name)
}

// QueryWebhookEndpoint queries the "webhook_endpoint" edge of the WebhookEvent entity.
func (we *WebhookEvent) QueryWebhookEndpoint() *WebhookEndpointQuery {
	return NewWebhookEventClient(we.config).QueryWebhookEndpoint(we)
}

// Update returns a builder for updating this WebhookEvent.
// Note that you need to call WebhookEvent.Unwrap() before calling this method if this WebhookEvent
// was returned from a transaction, and the transaction was committed or rolled back.
func (we *WebhookEvent) Update() *WebhookEventUpdateOne {
	return NewWebhookEventClient(we.config).UpdateOne(we)
}

// Unwrap unwraps the WebhookEvent entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (we *WebhookEvent) Unwrap() *WebhookEvent {
	_tx, ok := we.config.driver.(*txDriver)
	if !ok {
		panic("ent: WebhookEvent is not a transactional entity")
	}
	we.config.driver = _tx.drv
	return we
}

// String implements the fmt.Stringer.
func (we *WebhookEvent) String() string {
	var builder strings.Builder
	builder.WriteString("WebhookEvent(")
	builder.WriteString(fmt.Sprintf("id=%v, ", we.ID))
	builder.WriteString("webhook_endpoint_id=")
	builder.WriteString(we.WebhookEndpointID)
	builder.WriteString(", ")
	builder.WriteString("event_type=")
	builder.WriteString(we.EventType)
	builder.WriteString(", ")
	builder.WriteString("payload=")
	builder.WriteString(fmt.Sprintf("%v", we.Payload))
	builder.WriteString(", ")
	builder.WriteString("status_code=")
	builder.WriteString(fmt.Sprintf("%v", we.StatusCode))
	builder.WriteString(", ")
	builder.WriteString("response_body=")
	builder.WriteString(we.ResponseBody)
	builder.WriteString(", ")
	builder.WriteString("error_message=")
	builder.WriteString(we.ErrorMessage)
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", we.Status))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(we.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// WebhookEvents is a parsable slice of WebhookEvent.
type WebhookEvents []*WebhookEvent

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/user"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/useripsubnethistory"
)

// UserIpSubnetHistory is the model entity for the UserIpSubnetHistory schema.
type UserIpSubnetHistory struct {
	config `json:"-"`
	// ID of the ent.
	// Unique Subnet History ID (e.g. sub_1a2b3c)
	ID string `json:"id,omitempty"`
	// Owning User ID
	UserID string `json:"user_id,omitempty"`
	// Subnet CIDR string (e.g. 198.51.100.0/24 or 2001:db8:abcd::/48)
	Subnet string `json:"subnet,omitempty"`
	// IP protocol version: 4 or 6
	IPVersion int `json:"ip_version,omitempty"`
	// Total successful logins/authentications observed from this subnet
	LoginCount int `json:"login_count,omitempty"`
	// Timestamp when subnet was first observed
	FirstSeenAt time.Time `json:"first_seen_at,omitempty"`
	// Timestamp when subnet was last observed
	LastSeenAt time.Time `json:"last_seen_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the UserIpSubnetHistoryQuery when eager-loading is set.
	Edges        UserIpSubnetHistoryEdges `json:"edges"`
	selectValues sql.SelectValues
}

// UserIpSubnetHistoryEdges holds the relations/edges for other nodes in the graph.
type UserIpSubnetHistoryEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserIpSubnetHistoryEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UserIpSubnetHistory) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case useripsubnethistory.FieldIPVersion, useripsubnethistory.FieldLoginCount:
			values[i] = new(sql.NullInt64)
		case useripsubnethistory.FieldID, useripsubnethistory.FieldUserID, useripsubnethistory.FieldSubnet:
			values[i] = new(sql.NullString)
		case useripsubnethistory.FieldFirstSeenAt, useripsubnethistory.FieldLastSeenAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UserIpSubnetHistory fields.
func (uish *UserIpSubnetHistory) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case useripsubnethistory.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				uish.ID = value.String
			}
		case useripsubnethistory.FieldUserID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				uish.UserID = value.String
			}
		case useripsubnethistory.FieldSubnet:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field subnet", values[i])
			} else if value.Valid {
				uish.Subnet = value.String
			}
		case useripsubnethistory.FieldIPVersion:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field ip_version", values[i])
			} else if value.Valid {
				uish.IPVersion = int(value.Int64)
			}
		case useripsubnethistory.FieldLoginCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field login_count", values[i])
			} else if value.Valid {
				uish.LoginCount = int(value.Int64)
			}
		case useripsubnethistory.FieldFirstSeenAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field first_seen_at", values[i])
			} else if value.Valid {
				uish.FirstSeenAt = value.Time
			}
		case useripsubnethistory.FieldLastSeenAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_seen_at", values[i])
			} else if value.Valid {
				uish.LastSeenAt = value.Time
			}
		default:
			uish.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UserIpSubnetHistory.
// This includes values selected through modifiers, order, etc.
func (uish *UserIpSubnetHistory) Value(name string) (ent.Value, error) {
	return uish.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the UserIpSubnetHistory entity.
func (uish *UserIpSubnetHistory) QueryUser() *UserQuery {
	return NewUserIpSubnetHistoryClient(uish.config).QueryUser(uish)
}

// Update returns a builder for updating this UserIpSubnetHistory.
// Note that you need to call UserIpSubnetHistory.Unwrap() before calling this method if this UserIpSubnetHistory
// was returned from a transaction, and the transaction was committed or rolled back.
func (uish *UserIpSubnetHistory) Update() *UserIpSubnetHistoryUpdateOne {
	return NewUserIpSubnetHistoryClient(uish.config).UpdateOne(uish)
}

// Unwrap unwraps the UserIpSubnetHistory entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (uish *UserIpSubnetHistory) Unwrap() *UserIpSubnetHistory {
	_tx, ok := uish.config.driver.(*txDriver)
	if !ok {
		panic("ent: UserIpSubnetHistory is not a transactional entity")
	}
	uish.config.driver = _tx.drv
	return uish
}

// String implements the fmt.Stringer.
func (uish *UserIpSubnetHistory) String() string {
	var builder strings.Builder
	builder.WriteString("UserIpSubnetHistory(")
	builder.WriteString(fmt.Sprintf("id=%v, ", uish.ID))
	builder.WriteString("user_id=")
	builder.WriteString(uish.UserID)
	builder.WriteString(", ")
	builder.WriteString("subnet=")
	builder.WriteString(uish.Subnet)
	builder.WriteString(", ")
	builder.WriteString("ip_version=")
	builder.WriteString(fmt.Sprintf("%v", uish.IPVersion))
	builder.WriteString(", ")
	builder.WriteString("login_count=")
	builder.WriteString(fmt.Sprintf("%v", uish.LoginCount))
	builder.WriteString(", ")
	builder.WriteString("first_seen_at=")
	builder.WriteString(uish.FirstSeenAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("last_seen_at=")
	builder.WriteString(uish.LastSeenAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// UserIpSubnetHistories is a parsable slice of UserIpSubnetHistory.
type UserIpSubnetHistories []*UserIpSubnetHistory

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/organization"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/samlconnection"
)

// SAMLConnection is the model entity for the SAMLConnection schema.
type SAMLConnection struct {
	config `json:"-"`
	// ID of the ent.
	// Unique SAML Connection ID identifier (e.g. saml_1a2b3c)
	ID string `json:"id,omitempty"`
	// Owning Organization ID
	OrganizationID string `json:"organization_id,omitempty"`
	// SAML Identity Provider Entity ID / Issuer URI
	IdpEntityID string `json:"idp_entity_id,omitempty"`
	// SAML Identity Provider Single Sign-On HTTP-POST / Redirect URL
	IdpSSOURL string `json:"idp_sso_url,omitempty"`
	// Public X.509 PEM certificate used for verifying SAML assertion XML signatures
	IdpCertificate string `json:"idp_certificate,omitempty"`
	// Array of verified email domains associated with this SAML connection (e.g. ['siemens.com', 'siemens.de'])
	AllowedDomains []string `json:"allowed_domains,omitempty"`
	// JSON mapping of SAML assertion attribute names to Authn user fields
	AttributeMapping map[string]string `json:"attribute_mapping,omitempty"`
	// Flag indicating if password and social logins are strictly disabled for allowed domains
	EnforceSSO bool `json:"enforce_sso,omitempty"`
	// Environment that users authenticated through this connection are provisioned into. Defaults to live, unlike every other environment field in the schema, because a connection only exists once an administrator has pasted a real identity provider's certificate and SSO URL — the people arriving through it are that organization's actual employees. Point a connection at test to trial a new identity provider without minting live accounts.
	Environment samlconnection.Environment `json:"environment,omitempty"`
	// Creation timestamp
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Last update timestamp
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SAMLConnectionQuery when eager-loading is set.
	Edges        SAMLConnectionEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SAMLConnectionEdges holds the relations/edges for other nodes in the graph.
type SAMLConnectionEdges struct {
	// Organization holds the value of the organization edge.
	Organization *Organization `json:"organization,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// OrganizationOrErr returns the Organization value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SAMLConnectionEdges) OrganizationOrErr() (*Organization, error) {
	if e.Organization != nil {
		return e.Organization, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: organization.Label}
	}
	return nil, &NotLoadedError{edge: "organization"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SAMLConnection) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case samlconnection.FieldAllowedDomains, samlconnection.FieldAttributeMapping:
			values[i] = new([]byte)
		case samlconnection.FieldEnforceSSO:
			values[i] = new(sql.NullBool)
		case samlconnection.FieldID, samlconnection.FieldOrganizationID, samlconnection.FieldIdpEntityID, samlconnection.FieldIdpSSOURL, samlconnection.FieldIdpCertificate, samlconnection.FieldEnvironment:
			values[i] = new(sql.NullString)
		case samlconnection.FieldCreatedAt, samlconnection.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SAMLConnection fields.
func (sc *SAMLConnection) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case samlconnection.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				sc.ID = value.String
			}
		case samlconnection.FieldOrganizationID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field organization_id", values[i])
			} else if value.Valid {
				sc.OrganizationID = value.String
			}
		case samlconnection.FieldIdpEntityID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field idp_entity_id", values[i])
			} else if value.Valid {
				sc.IdpEntityID = value.String
			}
		case samlconnection.FieldIdpSSOURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field idp_sso_url", values[i])
			} else if value.Valid {
				sc.IdpSSOURL = value.String
			}
		case samlconnection.FieldIdpCertificate:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field idp_certificate", values[i])
			} else if value.Valid {
				sc.IdpCertificate = value.String
			}
		case samlconnection.FieldAllowedDomains:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field allowed_domains", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &sc.AllowedDomains); err != nil {
					return fmt.Errorf("unmarshal field allowed_domains: %w", err)
				}
			}
		case samlconnection.FieldAttributeMapping:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field attribute_mapping", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &sc.AttributeMapping); err != nil {
					return fmt.Errorf("unmarshal field attribute_mapping: %w", err)
				}
			}
		case samlconnection.FieldEnforceSSO:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field enforce_sso", values[i])
			} else if value.Valid {
				sc.EnforceSSO = value.Bool
			}
		case samlconnection.FieldEnvironment:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field environment", values[i])
			} else if value.Valid {
				sc.Environment = samlconnection.Environment(value.String)
			}
		case samlconnection.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				sc.CreatedAt = value.Time
			}
		case samlconnection.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				sc.UpdatedAt = value.Time
			}
		default:
			sc.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SAMLConnection.
// This includes values selected through modifiers, order, etc.
func (sc *SAMLConnection) Value(name string) (ent.Value, error) {
	return sc.selectValues.Get(name)
}

// QueryOrganization queries the "organization" edge of the SAMLConnection entity.
func (sc *SAMLConnection) QueryOrganization() *OrganizationQuery {
	return NewSAMLConnectionClient(sc.config).QueryOrganization(sc)
}

// Update returns a builder for updating this SAMLConnection.
// Note that you need to call SAMLConnection.Unwrap() before calling this method if this SAMLConnection
// was returned from a transaction, and the transaction was committed or rolled back.
func (sc *SAMLConnection) Update() *SAMLConnectionUpdateOne {
	return NewSAMLConnectionClient(sc.config).UpdateOne(sc)
}

// Unwrap unwraps the SAMLConnection entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sc *SAMLConnection) Unwrap() *SAMLConnection {
	_tx, ok := sc.config.driver.(*txDriver)
	if !ok {
		panic("ent: SAMLConnection is not a transactional entity")
	}
	sc.config.driver = _tx.drv
	return sc
}

// String implements the fmt.Stringer.
func (sc *SAMLConnection) String() string {
	var builder strings.Builder
	builder.WriteString("SAMLConnection(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sc.ID))
	builder.WriteString("organization_id=")
	builder.WriteString(sc.OrganizationID)
	builder.WriteString(", ")
	builder.WriteString("idp_entity_id=")
	builder.WriteString(sc.IdpEntityID)
	builder.WriteString(", ")
	builder.WriteString("idp_sso_url=")
	builder.WriteString(sc.IdpSSOURL)
	builder.WriteString(", ")
	builder.WriteString("idp_certificate=")
	builder.WriteString(sc.IdpCertificate)
	builder.WriteString(", ")
	builder.WriteString("allowed_domains=")
	builder.WriteString(fmt.Sprintf("%v", sc.AllowedDomains))
	builder.WriteString(", ")
	builder.WriteString("attribute_mapping=")
	builder.WriteString(fmt.Sprintf("%v", sc.AttributeMapping))
	builder.WriteString(", ")
	builder.WriteString("enforce_sso=")
	builder.WriteString(fmt.Sprintf("%v", sc.EnforceSSO))
	builder.WriteString(", ")
	builder.WriteString("environment=")
	builder.WriteString(fmt.Sprintf("%v", sc.Environment))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(sc.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(sc.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SAMLConnections is a parsable slice of SAMLConnection.
type SAMLConnections []*SAMLConnection

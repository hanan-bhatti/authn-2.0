// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/application"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/session"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/sessionappactivity"
)

// SessionAppActivity is the model entity for the SessionAppActivity schema.
type SessionAppActivity struct {
	config `json:"-"`
	// ID of the ent.
	// Unique Session App Activity ID identifier (e.g. saa_1a2b3c)
	ID string `json:"id,omitempty"`
	// Session this activity belongs to. Re-pointed at the successor when a refresh rotates the session, so activity follows the login rather than restarting every rotation
	SessionID string `json:"session_id,omitempty"`
	// Application the session was used at
	ApplicationID string `json:"application_id,omitempty"`
	// Timestamp when this session was first used at this application
	FirstSeenAt time.Time `json:"first_seen_at,omitempty"`
	// Timestamp when this session was most recently used at this application
	LastActiveAt time.Time `json:"last_active_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SessionAppActivityQuery when eager-loading is set.
	Edges        SessionAppActivityEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SessionAppActivityEdges holds the relations/edges for other nodes in the graph.
type SessionAppActivityEdges struct {
	// Session holds the value of the session edge.
	Session *Session `json:"session,omitempty"`
	// Application holds the value of the application edge.
	Application *Application `json:"application,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// SessionOrErr returns the Session value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SessionAppActivityEdges) SessionOrErr() (*Session, error) {
	if e.Session != nil {
		return e.Session, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: session.Label}
	}
	return nil, &NotLoadedError{edge: "session"}
}

// ApplicationOrErr returns the Application value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SessionAppActivityEdges) ApplicationOrErr() (*Application, error) {
	if e.Application != nil {
		return e.Application, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: application.Label}
	}
	return nil, &NotLoadedError{edge: "application"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SessionAppActivity) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case sessionappactivity.FieldID, sessionappactivity.FieldSessionID, sessionappactivity.FieldApplicationID:
			values[i] = new(sql.NullString)
		case sessionappactivity.FieldFirstSeenAt, sessionappactivity.FieldLastActiveAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SessionAppActivity fields.
func (saa *SessionAppActivity) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case sessionappactivity.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				saa.ID = value.String
			}
		case sessionappactivity.FieldSessionID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field session_id", values[i])
			} else if value.Valid {
				saa.SessionID = value.String
			}
		case sessionappactivity.FieldApplicationID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field application_id", values[i])
			} else if value.Valid {
				saa.ApplicationID = value.String
			}
		case sessionappactivity.FieldFirstSeenAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field first_seen_at", values[i])
			} else if value.Valid {
				saa.FirstSeenAt = value.Time
			}
		case sessionappactivity.FieldLastActiveAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_active_at", values[i])
			} else if value.Valid {
				saa.LastActiveAt = value.Time
			}
		default:
			saa.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SessionAppActivity.
// This includes values selected through modifiers, order, etc.
func (saa *SessionAppActivity) Value(name string) (ent.Value, error) {
	return saa.selectValues.Get(name)
}

// QuerySession queries the "session" edge of the SessionAppActivity entity.
func (saa *SessionAppActivity) QuerySession() *SessionQuery {
	return NewSessionAppActivityClient(saa.config).QuerySession(saa)
}

// QueryApplication queries the "application" edge of the SessionAppActivity entity.
func (saa *SessionAppActivity) QueryApplication() *ApplicationQuery {
	return NewSessionAppActivityClient(saa.config).QueryApplication(saa)
}

// Update returns a builder for updating this SessionAppActivity.
// Note that you need to call SessionAppActivity.Unwrap() before calling this method if this SessionAppActivity
// was returned from a transaction, and the transaction was committed or rolled back.
func (saa *SessionAppActivity) Update() *SessionAppActivityUpdateOne {
	return NewSessionAppActivityClient(saa.config).UpdateOne(saa)
}

// Unwrap unwraps the SessionAppActivity entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (saa *SessionAppActivity) Unwrap() *SessionAppActivity {
	_tx, ok := saa.config.driver.(*txDriver)
	if !ok {
		panic("ent: SessionAppActivity is not a transactional entity")
	}
	saa.config.driver = _tx.drv
	return saa
}

// String implements the fmt.Stringer.
func (saa *SessionAppActivity) String() string {
	var builder strings.Builder
	builder.WriteString("SessionAppActivity(")
	builder.WriteString(fmt.Sprintf("id=%v, ", saa.ID))
	builder.WriteString("session_id=")
	builder.WriteString(saa.SessionID)
	builder.WriteString(", ")
	builder.WriteString("application_id=")
	builder.WriteString(saa.ApplicationID)
	builder.WriteString(", ")
	builder.WriteString("first_seen_at=")
	builder.WriteString(saa.FirstSeenAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("last_active_at=")
	builder.WriteString(saa.LastActiveAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SessionAppActivities is a parsable slice of SessionAppActivity.
type SessionAppActivities []*SessionAppActivity

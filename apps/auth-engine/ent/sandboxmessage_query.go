// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/predicate"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/sandboxmessage"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/tenant"
)

// SandboxMessageQuery is the builder for querying SandboxMessage entities.
type SandboxMessageQuery struct {
	config
	ctx        *QueryContext
	order      []sandboxmessage.OrderOption
	inters     []Interceptor
	predicates []predicate.SandboxMessage
	withTenant *TenantQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SandboxMessageQuery builder.
func (smq *SandboxMessageQuery) Where(ps ...predicate.SandboxMessage) *SandboxMessageQuery {
	smq.predicates = append(smq.predicates, ps...)
	return smq
}

// Limit the number of records to be returned by this query.
func (smq *SandboxMessageQuery) Limit(limit int) *SandboxMessageQuery {
	smq.ctx.Limit = &limit
	return smq
}

// Offset to start from.
func (smq *SandboxMessageQuery) Offset(offset int) *SandboxMessageQuery {
	smq.ctx.Offset = &offset
	return smq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (smq *SandboxMessageQuery) Unique(unique bool) *SandboxMessageQuery {
	smq.ctx.Unique = &unique
	return smq
}

// Order specifies how the records should be ordered.
func (smq *SandboxMessageQuery) Order(o ...sandboxmessage.OrderOption) *SandboxMessageQuery {
	smq.order = append(smq.order, o...)
	return smq
}

// QueryTenant chains the current query on the "tenant" edge.
func (smq *SandboxMessageQuery) QueryTenant() *TenantQuery {
	query := (&TenantClient{config: smq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := smq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := smq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(sandboxmessage.Table, sandboxmessage.FieldID, selector),
			sqlgraph.To(tenant.Table, tenant.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, sandboxmessage.TenantTable, sandboxmessage.TenantColumn),
		)
		fromU = sqlgraph.SetNeighbors(smq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first SandboxMessage entity from the query.
// Returns a *NotFoundError when no SandboxMessage was found.
func (smq *SandboxMessageQuery) First(ctx context.Context) (*SandboxMessage, error) {
	nodes, err := smq.Limit(1).All(setContextOp(ctx, smq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{sandboxmessage.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (smq *SandboxMessageQuery) FirstX(ctx context.Context) *SandboxMessage {
	node, err := smq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SandboxMessage ID from the query.
// Returns a *NotFoundError when no SandboxMessage ID was found.
func (smq *SandboxMessageQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = smq.Limit(1).IDs(setContextOp(ctx, smq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{sandboxmessage.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (smq *SandboxMessageQuery) FirstIDX(ctx context.Context) string {
	id, err := smq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SandboxMessage entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SandboxMessage entity is found.
// Returns a *NotFoundError when no SandboxMessage entities are found.
func (smq *SandboxMessageQuery) Only(ctx context.Context) (*SandboxMessage, error) {
	nodes, err := smq.Limit(2).All(setContextOp(ctx, smq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{sandboxmessage.Label}
	default:
		return nil, &NotSingularError{sandboxmessage.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (smq *SandboxMessageQuery) OnlyX(ctx context.Context) *SandboxMessage {
	node, err := smq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SandboxMessage ID in the query.
// Returns a *NotSingularError when more than one SandboxMessage ID is found.
// Returns a *NotFoundError when no entities are found.
func (smq *SandboxMessageQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = smq.Limit(2).IDs(setContextOp(ctx, smq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{sandboxmessage.Label}
	default:
		err = &NotSingularError{sandboxmessage.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (smq *SandboxMessageQuery) OnlyIDX(ctx context.Context) string {
	id, err := smq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SandboxMessages.
func (smq *SandboxMessageQuery) All(ctx context.Context) ([]*SandboxMessage, error) {
	ctx = setContextOp(ctx, smq.ctx, ent.OpQueryAll)
	if err := smq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SandboxMessage, *SandboxMessageQuery]()
	return withInterceptors[[]*SandboxMessage](ctx, smq, qr, smq.inters)
}

// AllX is like All, but panics if an error occurs.
func (smq *SandboxMessageQuery) AllX(ctx context.Context) []*SandboxMessage {
	nodes, err := smq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SandboxMessage IDs.
func (smq *SandboxMessageQuery) IDs(ctx context.Context) (ids []string, err error) {
	if smq.ctx.Unique == nil && smq.path != nil {
		smq.Unique(true)
	}
	ctx = setContextOp(ctx, smq.ctx, ent.OpQueryIDs)
	if err = smq.Select(sandboxmessage.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (smq *SandboxMessageQuery) IDsX(ctx context.Context) []string {
	ids, err := smq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (smq *SandboxMessageQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, smq.ctx, ent.OpQueryCount)
	if err := smq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, smq, querierCount[*SandboxMessageQuery](), smq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (smq *SandboxMessageQuery) CountX(ctx context.Context) int {
	count, err := smq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (smq *SandboxMessageQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, smq.ctx, ent.OpQueryExist)
	switch _, err := smq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (smq *SandboxMessageQuery) ExistX(ctx context.Context) bool {
	exist, err := smq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SandboxMessageQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (smq *SandboxMessageQuery) Clone() *SandboxMessageQuery {
	if smq == nil {
		return nil
	}
	return &SandboxMessageQuery{
		config:     smq.config,
		ctx:        smq.ctx.Clone(),
		order:      append([]sandboxmessage.OrderOption{}, smq.order...),
		inters:     append([]Interceptor{}, smq.inters...),
		predicates: append([]predicate.SandboxMessage{}, smq.predicates...),
		withTenant: smq.withTenant.Clone(),
		// clone intermediate query.
		sql:  smq.sql.Clone(),
		path: smq.path,
	}
}

// WithTenant tells the query-builder to eager-load the nodes that are connected to
// the "tenant" edge. The optional arguments are used to configure the query builder of the edge.
func (smq *SandboxMessageQuery) WithTenant(opts ...func(*TenantQuery)) *SandboxMessageQuery {
	query := (&TenantClient{config: smq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	smq.withTenant = query
	return smq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID string `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SandboxMessage.Query().
//		GroupBy(sandboxmessage.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (smq *SandboxMessageQuery) GroupBy(field string, fields ...string) *SandboxMessageGroupBy {
	smq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SandboxMessageGroupBy{build: smq}
	grbuild.flds = &smq.ctx.Fields
	grbuild.label = sandboxmessage.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID string `json:"tenant_id,omitempty"`
//	}
//
//	client.SandboxMessage.Query().
//		Select(sandboxmessage.FieldTenantID).
//		Scan(ctx, &v)
func (smq *SandboxMessageQuery) Select(fields ...string) *SandboxMessageSelect {
	smq.ctx.Fields = append(smq.ctx.Fields, fields...)
	sbuild := &SandboxMessageSelect{SandboxMessageQuery: smq}
	sbuild.label = sandboxmessage.Label
	sbuild.flds, sbuild.scan = &smq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SandboxMessageSelect configured with the given aggregations.
func (smq *SandboxMessageQuery) Aggregate(fns ...AggregateFunc) *SandboxMessageSelect {
	return smq.Select().Aggregate(fns...)
}

func (smq *SandboxMessageQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range smq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, smq); err != nil {
				return err
			}
		}
	}
	for _, f := range smq.ctx.Fields {
		if !sandboxmessage.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if smq.path != nil {
		prev, err := smq.path(ctx)
		if err != nil {
			return err
		}
		smq.sql = prev
	}
	return nil
}

func (smq *SandboxMessageQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SandboxMessage, error) {
	var (
		nodes       = []*SandboxMessage{}
		_spec       = smq.querySpec()
		loadedTypes = [1]bool{
			smq.withTenant != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SandboxMessage).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SandboxMessage{config: smq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, smq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := smq.withTenant; query != nil {
		if err := smq.loadTenant(ctx, query, nodes, nil,
			func(n *SandboxMessage, e *Tenant) { n.Edges.Tenant = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (smq *SandboxMessageQuery) loadTenant(ctx context.Context, query *TenantQuery, nodes []*SandboxMessage, init func(*SandboxMessage), assign func(*SandboxMessage, *Tenant)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*SandboxMessage)
	for i := range nodes {
		fk := nodes[i].TenantID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(tenant.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "tenant_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (smq *SandboxMessageQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := smq.querySpec()
	_spec.Node.Columns = smq.ctx.Fields
	if len(smq.ctx.Fields) > 0 {
		_spec.Unique = smq.ctx.Unique != nil && *smq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, smq.driver, _spec)
}

func (smq *SandboxMessageQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(sandboxmessage.Table, sandboxmessage.Columns, sqlgraph.NewFieldSpec(sandboxmessage.FieldID, field.TypeString))
	_spec.From = smq.sql
	if unique := smq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if smq.path != nil {
		_spec.Unique = true
	}
	if fields := smq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, sandboxmessage.FieldID)
		for i := range fields {
			if fields[i] != sandboxmessage.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if smq.withTenant != nil {
			_spec.Node.AddColumnOnce(sandboxmessage.FieldTenantID)
		}
	}
	if ps := smq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := smq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := smq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := smq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (smq *SandboxMessageQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(smq.driver.Dialect())
	t1 := builder.Table(sandboxmessage.Table)
	columns := smq.ctx.Fields
	if len(columns) == 0 {
		columns = sandboxmessage.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if smq.sql != nil {
		selector = smq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if smq.ctx.Unique != nil && *smq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range smq.predicates {
		p(selector)
	}
	for _, p := range smq.order {
		p(selector)
	}
	if offset := smq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := smq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SandboxMessageGroupBy is the group-by builder for SandboxMessage entities.
type SandboxMessageGroupBy struct {
	selector
	build *SandboxMessageQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (smgb *SandboxMessageGroupBy) Aggregate(fns ...AggregateFunc) *SandboxMessageGroupBy {
	smgb.fns = append(smgb.fns, fns...)
	return smgb
}

// Scan applies the selector query and scans the result into the given value.
func (smgb *SandboxMessageGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, smgb.build.ctx, ent.OpQueryGroupBy)
	if err := smgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SandboxMessageQuery, *SandboxMessageGroupBy](ctx, smgb.build, smgb, smgb.build.inters, v)
}

func (smgb *SandboxMessageGroupBy) sqlScan(ctx context.Context, root *SandboxMessageQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(smgb.fns))
	for _, fn := range smgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*smgb.flds)+len(smgb.fns))
		for _, f := range *smgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*smgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := smgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SandboxMessageSelect is the builder for selecting fields of SandboxMessage entities.
type SandboxMessageSelect struct {
	*SandboxMessageQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sms *SandboxMessageSelect) Aggregate(fns ...AggregateFunc) *SandboxMessageSelect {
	sms.fns = append(sms.fns, fns...)
	return sms
}

// Scan applies the selector query and scans the result into the given value.
func (sms *SandboxMessageSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sms.ctx, ent.OpQuerySelect)
	if err := sms.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SandboxMessageQuery, *SandboxMessageSelect](ctx, sms.SandboxMessageQuery, sms, sms.inters, v)
}

func (sms *SandboxMessageSelect) sqlScan(ctx context.Context, root *SandboxMessageQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sms.fns))
	for _, fn := range sms.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sms.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sms.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

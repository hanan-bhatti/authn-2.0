// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/predicate"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/recoverycontact"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/user"
)

// RecoveryContactQuery is the builder for querying RecoveryContact entities.
type RecoveryContactQuery struct {
	config
	ctx        *QueryContext
	order      []recoverycontact.OrderOption
	inters     []Interceptor
	predicates []predicate.RecoveryContact
	withUser   *UserQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RecoveryContactQuery builder.
func (rcq *RecoveryContactQuery) Where(ps ...predicate.RecoveryContact) *RecoveryContactQuery {
	rcq.predicates = append(rcq.predicates, ps...)
	return rcq
}

// Limit the number of records to be returned by this query.
func (rcq *RecoveryContactQuery) Limit(limit int) *RecoveryContactQuery {
	rcq.ctx.Limit = &limit
	return rcq
}

// Offset to start from.
func (rcq *RecoveryContactQuery) Offset(offset int) *RecoveryContactQuery {
	rcq.ctx.Offset = &offset
	return rcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rcq *RecoveryContactQuery) Unique(unique bool) *RecoveryContactQuery {
	rcq.ctx.Unique = &unique
	return rcq
}

// Order specifies how the records should be ordered.
func (rcq *RecoveryContactQuery) Order(o ...recoverycontact.OrderOption) *RecoveryContactQuery {
	rcq.order = append(rcq.order, o...)
	return rcq
}

// QueryUser chains the current query on the "user" edge.
func (rcq *RecoveryContactQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: rcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(recoverycontact.Table, recoverycontact.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, recoverycontact.UserTable, recoverycontact.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(rcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first RecoveryContact entity from the query.
// Returns a *NotFoundError when no RecoveryContact was found.
func (rcq *RecoveryContactQuery) First(ctx context.Context) (*RecoveryContact, error) {
	nodes, err := rcq.Limit(1).All(setContextOp(ctx, rcq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{recoverycontact.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rcq *RecoveryContactQuery) FirstX(ctx context.Context) *RecoveryContact {
	node, err := rcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RecoveryContact ID from the query.
// Returns a *NotFoundError when no RecoveryContact ID was found.
func (rcq *RecoveryContactQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = rcq.Limit(1).IDs(setContextOp(ctx, rcq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{recoverycontact.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rcq *RecoveryContactQuery) FirstIDX(ctx context.Context) string {
	id, err := rcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RecoveryContact entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RecoveryContact entity is found.
// Returns a *NotFoundError when no RecoveryContact entities are found.
func (rcq *RecoveryContactQuery) Only(ctx context.Context) (*RecoveryContact, error) {
	nodes, err := rcq.Limit(2).All(setContextOp(ctx, rcq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{recoverycontact.Label}
	default:
		return nil, &NotSingularError{recoverycontact.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rcq *RecoveryContactQuery) OnlyX(ctx context.Context) *RecoveryContact {
	node, err := rcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RecoveryContact ID in the query.
// Returns a *NotSingularError when more than one RecoveryContact ID is found.
// Returns a *NotFoundError when no entities are found.
func (rcq *RecoveryContactQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = rcq.Limit(2).IDs(setContextOp(ctx, rcq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{recoverycontact.Label}
	default:
		err = &NotSingularError{recoverycontact.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rcq *RecoveryContactQuery) OnlyIDX(ctx context.Context) string {
	id, err := rcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RecoveryContacts.
func (rcq *RecoveryContactQuery) All(ctx context.Context) ([]*RecoveryContact, error) {
	ctx = setContextOp(ctx, rcq.ctx, ent.OpQueryAll)
	if err := rcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RecoveryContact, *RecoveryContactQuery]()
	return withInterceptors[[]*RecoveryContact](ctx, rcq, qr, rcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rcq *RecoveryContactQuery) AllX(ctx context.Context) []*RecoveryContact {
	nodes, err := rcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RecoveryContact IDs.
func (rcq *RecoveryContactQuery) IDs(ctx context.Context) (ids []string, err error) {
	if rcq.ctx.Unique == nil && rcq.path != nil {
		rcq.Unique(true)
	}
	ctx = setContextOp(ctx, rcq.ctx, ent.OpQueryIDs)
	if err = rcq.Select(recoverycontact.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rcq *RecoveryContactQuery) IDsX(ctx context.Context) []string {
	ids, err := rcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rcq *RecoveryContactQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rcq.ctx, ent.OpQueryCount)
	if err := rcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rcq, querierCount[*RecoveryContactQuery](), rcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rcq *RecoveryContactQuery) CountX(ctx context.Context) int {
	count, err := rcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rcq *RecoveryContactQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rcq.ctx, ent.OpQueryExist)
	switch _, err := rcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rcq *RecoveryContactQuery) ExistX(ctx context.Context) bool {
	exist, err := rcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RecoveryContactQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rcq *RecoveryContactQuery) Clone() *RecoveryContactQuery {
	if rcq == nil {
		return nil
	}
	return &RecoveryContactQuery{
		config:     rcq.config,
		ctx:        rcq.ctx.Clone(),
		order:      append([]recoverycontact.OrderOption{}, rcq.order...),
		inters:     append([]Interceptor{}, rcq.inters...),
		predicates: append([]predicate.RecoveryContact{}, rcq.predicates...),
		withUser:   rcq.withUser.Clone(),
		// clone intermediate query.
		sql:  rcq.sql.Clone(),
		path: rcq.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (rcq *RecoveryContactQuery) WithUser(opts ...func(*UserQuery)) *RecoveryContactQuery {
	query := (&UserClient{config: rcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rcq.withUser = query
	return rcq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID string `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RecoveryContact.Query().
//		GroupBy(recoverycontact.FieldUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (rcq *RecoveryContactQuery) GroupBy(field string, fields ...string) *RecoveryContactGroupBy {
	rcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RecoveryContactGroupBy{build: rcq}
	grbuild.flds = &rcq.ctx.Fields
	grbuild.label = recoverycontact.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID string `json:"user_id,omitempty"`
//	}
//
//	client.RecoveryContact.Query().
//		Select(recoverycontact.FieldUserID).
//		Scan(ctx, &v)
func (rcq *RecoveryContactQuery) Select(fields ...string) *RecoveryContactSelect {
	rcq.ctx.Fields = append(rcq.ctx.Fields, fields...)
	sbuild := &RecoveryContactSelect{RecoveryContactQuery: rcq}
	sbuild.label = recoverycontact.Label
	sbuild.flds, sbuild.scan = &rcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RecoveryContactSelect configured with the given aggregations.
func (rcq *RecoveryContactQuery) Aggregate(fns ...AggregateFunc) *RecoveryContactSelect {
	return rcq.Select().Aggregate(fns...)
}

func (rcq *RecoveryContactQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rcq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rcq); err != nil {
				return err
			}
		}
	}
	for _, f := range rcq.ctx.Fields {
		if !recoverycontact.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if rcq.path != nil {
		prev, err := rcq.path(ctx)
		if err != nil {
			return err
		}
		rcq.sql = prev
	}
	return nil
}

func (rcq *RecoveryContactQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RecoveryContact, error) {
	var (
		nodes       = []*RecoveryContact{}
		_spec       = rcq.querySpec()
		loadedTypes = [1]bool{
			rcq.withUser != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RecoveryContact).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RecoveryContact{config: rcq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := rcq.withUser; query != nil {
		if err := rcq.loadUser(ctx, query, nodes, nil,
			func(n *RecoveryContact, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (rcq *RecoveryContactQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*RecoveryContact, init func(*RecoveryContact), assign func(*RecoveryContact, *User)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*RecoveryContact)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (rcq *RecoveryContactQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rcq.querySpec()
	_spec.Node.Columns = rcq.ctx.Fields
	if len(rcq.ctx.Fields) > 0 {
		_spec.Unique = rcq.ctx.Unique != nil && *rcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rcq.driver, _spec)
}

func (rcq *RecoveryContactQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(recoverycontact.Table, recoverycontact.Columns, sqlgraph.NewFieldSpec(recoverycontact.FieldID, field.TypeString))
	_spec.From = rcq.sql
	if unique := rcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if rcq.path != nil {
		_spec.Unique = true
	}
	if fields := rcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, recoverycontact.FieldID)
		for i := range fields {
			if fields[i] != recoverycontact.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if rcq.withUser != nil {
			_spec.Node.AddColumnOnce(recoverycontact.FieldUserID)
		}
	}
	if ps := rcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rcq *RecoveryContactQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rcq.driver.Dialect())
	t1 := builder.Table(recoverycontact.Table)
	columns := rcq.ctx.Fields
	if len(columns) == 0 {
		columns = recoverycontact.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rcq.sql != nil {
		selector = rcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rcq.ctx.Unique != nil && *rcq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range rcq.predicates {
		p(selector)
	}
	for _, p := range rcq.order {
		p(selector)
	}
	if offset := rcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// RecoveryContactGroupBy is the group-by builder for RecoveryContact entities.
type RecoveryContactGroupBy struct {
	selector
	build *RecoveryContactQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rcgb *RecoveryContactGroupBy) Aggregate(fns ...AggregateFunc) *RecoveryContactGroupBy {
	rcgb.fns = append(rcgb.fns, fns...)
	return rcgb
}

// Scan applies the selector query and scans the result into the given value.
func (rcgb *RecoveryContactGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rcgb.build.ctx, ent.OpQueryGroupBy)
	if err := rcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RecoveryContactQuery, *RecoveryContactGroupBy](ctx, rcgb.build, rcgb, rcgb.build.inters, v)
}

func (rcgb *RecoveryContactGroupBy) sqlScan(ctx context.Context, root *RecoveryContactQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rcgb.fns))
	for _, fn := range rcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rcgb.flds)+len(rcgb.fns))
		for _, f := range *rcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RecoveryContactSelect is the builder for selecting fields of RecoveryContact entities.
type RecoveryContactSelect struct {
	*RecoveryContactQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rcs *RecoveryContactSelect) Aggregate(fns ...AggregateFunc) *RecoveryContactSelect {
	rcs.fns = append(rcs.fns, fns...)
	return rcs
}

// Scan applies the selector query and scans the result into the given value.
func (rcs *RecoveryContactSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rcs.ctx, ent.OpQuerySelect)
	if err := rcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RecoveryContactQuery, *RecoveryContactSelect](ctx, rcs.RecoveryContactQuery, rcs, rcs.inters, v)
}

func (rcs *RecoveryContactSelect) sqlScan(ctx context.Context, root *RecoveryContactQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rcs.fns))
	for _, fn := range rcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

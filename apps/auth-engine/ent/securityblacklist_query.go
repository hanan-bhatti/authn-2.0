// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/predicate"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/securityblacklist"
)

// SecurityBlacklistQuery is the builder for querying SecurityBlacklist entities.
type SecurityBlacklistQuery struct {
	config
	ctx        *QueryContext
	order      []securityblacklist.OrderOption
	inters     []Interceptor
	predicates []predicate.SecurityBlacklist
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SecurityBlacklistQuery builder.
func (sbq *SecurityBlacklistQuery) Where(ps ...predicate.SecurityBlacklist) *SecurityBlacklistQuery {
	sbq.predicates = append(sbq.predicates, ps...)
	return sbq
}

// Limit the number of records to be returned by this query.
func (sbq *SecurityBlacklistQuery) Limit(limit int) *SecurityBlacklistQuery {
	sbq.ctx.Limit = &limit
	return sbq
}

// Offset to start from.
func (sbq *SecurityBlacklistQuery) Offset(offset int) *SecurityBlacklistQuery {
	sbq.ctx.Offset = &offset
	return sbq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (sbq *SecurityBlacklistQuery) Unique(unique bool) *SecurityBlacklistQuery {
	sbq.ctx.Unique = &unique
	return sbq
}

// Order specifies how the records should be ordered.
func (sbq *SecurityBlacklistQuery) Order(o ...securityblacklist.OrderOption) *SecurityBlacklistQuery {
	sbq.order = append(sbq.order, o...)
	return sbq
}

// First returns the first SecurityBlacklist entity from the query.
// Returns a *NotFoundError when no SecurityBlacklist was found.
func (sbq *SecurityBlacklistQuery) First(ctx context.Context) (*SecurityBlacklist, error) {
	nodes, err := sbq.Limit(1).All(setContextOp(ctx, sbq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{securityblacklist.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (sbq *SecurityBlacklistQuery) FirstX(ctx context.Context) *SecurityBlacklist {
	node, err := sbq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SecurityBlacklist ID from the query.
// Returns a *NotFoundError when no SecurityBlacklist ID was found.
func (sbq *SecurityBlacklistQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = sbq.Limit(1).IDs(setContextOp(ctx, sbq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{securityblacklist.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (sbq *SecurityBlacklistQuery) FirstIDX(ctx context.Context) string {
	id, err := sbq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SecurityBlacklist entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SecurityBlacklist entity is found.
// Returns a *NotFoundError when no SecurityBlacklist entities are found.
func (sbq *SecurityBlacklistQuery) Only(ctx context.Context) (*SecurityBlacklist, error) {
	nodes, err := sbq.Limit(2).All(setContextOp(ctx, sbq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{securityblacklist.Label}
	default:
		return nil, &NotSingularError{securityblacklist.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (sbq *SecurityBlacklistQuery) OnlyX(ctx context.Context) *SecurityBlacklist {
	node, err := sbq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SecurityBlacklist ID in the query.
// Returns a *NotSingularError when more than one SecurityBlacklist ID is found.
// Returns a *NotFoundError when no entities are found.
func (sbq *SecurityBlacklistQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = sbq.Limit(2).IDs(setContextOp(ctx, sbq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{securityblacklist.Label}
	default:
		err = &NotSingularError{securityblacklist.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (sbq *SecurityBlacklistQuery) OnlyIDX(ctx context.Context) string {
	id, err := sbq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SecurityBlacklists.
func (sbq *SecurityBlacklistQuery) All(ctx context.Context) ([]*SecurityBlacklist, error) {
	ctx = setContextOp(ctx, sbq.ctx, ent.OpQueryAll)
	if err := sbq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SecurityBlacklist, *SecurityBlacklistQuery]()
	return withInterceptors[[]*SecurityBlacklist](ctx, sbq, qr, sbq.inters)
}

// AllX is like All, but panics if an error occurs.
func (sbq *SecurityBlacklistQuery) AllX(ctx context.Context) []*SecurityBlacklist {
	nodes, err := sbq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SecurityBlacklist IDs.
func (sbq *SecurityBlacklistQuery) IDs(ctx context.Context) (ids []string, err error) {
	if sbq.ctx.Unique == nil && sbq.path != nil {
		sbq.Unique(true)
	}
	ctx = setContextOp(ctx, sbq.ctx, ent.OpQueryIDs)
	if err = sbq.Select(securityblacklist.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (sbq *SecurityBlacklistQuery) IDsX(ctx context.Context) []string {
	ids, err := sbq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (sbq *SecurityBlacklistQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, sbq.ctx, ent.OpQueryCount)
	if err := sbq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, sbq, querierCount[*SecurityBlacklistQuery](), sbq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (sbq *SecurityBlacklistQuery) CountX(ctx context.Context) int {
	count, err := sbq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (sbq *SecurityBlacklistQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, sbq.ctx, ent.OpQueryExist)
	switch _, err := sbq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (sbq *SecurityBlacklistQuery) ExistX(ctx context.Context) bool {
	exist, err := sbq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SecurityBlacklistQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (sbq *SecurityBlacklistQuery) Clone() *SecurityBlacklistQuery {
	if sbq == nil {
		return nil
	}
	return &SecurityBlacklistQuery{
		config:     sbq.config,
		ctx:        sbq.ctx.Clone(),
		order:      append([]securityblacklist.OrderOption{}, sbq.order...),
		inters:     append([]Interceptor{}, sbq.inters...),
		predicates: append([]predicate.SecurityBlacklist{}, sbq.predicates...),
		// clone intermediate query.
		sql:  sbq.sql.Clone(),
		path: sbq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID string `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SecurityBlacklist.Query().
//		GroupBy(securityblacklist.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (sbq *SecurityBlacklistQuery) GroupBy(field string, fields ...string) *SecurityBlacklistGroupBy {
	sbq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SecurityBlacklistGroupBy{build: sbq}
	grbuild.flds = &sbq.ctx.Fields
	grbuild.label = securityblacklist.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID string `json:"tenant_id,omitempty"`
//	}
//
//	client.SecurityBlacklist.Query().
//		Select(securityblacklist.FieldTenantID).
//		Scan(ctx, &v)
func (sbq *SecurityBlacklistQuery) Select(fields ...string) *SecurityBlacklistSelect {
	sbq.ctx.Fields = append(sbq.ctx.Fields, fields...)
	sbuild := &SecurityBlacklistSelect{SecurityBlacklistQuery: sbq}
	sbuild.label = securityblacklist.Label
	sbuild.flds, sbuild.scan = &sbq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SecurityBlacklistSelect configured with the given aggregations.
func (sbq *SecurityBlacklistQuery) Aggregate(fns ...AggregateFunc) *SecurityBlacklistSelect {
	return sbq.Select().Aggregate(fns...)
}

func (sbq *SecurityBlacklistQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range sbq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, sbq); err != nil {
				return err
			}
		}
	}
	for _, f := range sbq.ctx.Fields {
		if !securityblacklist.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if sbq.path != nil {
		prev, err := sbq.path(ctx)
		if err != nil {
			return err
		}
		sbq.sql = prev
	}
	return nil
}

func (sbq *SecurityBlacklistQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SecurityBlacklist, error) {
	var (
		nodes = []*SecurityBlacklist{}
		_spec = sbq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SecurityBlacklist).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SecurityBlacklist{config: sbq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, sbq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (sbq *SecurityBlacklistQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := sbq.querySpec()
	_spec.Node.Columns = sbq.ctx.Fields
	if len(sbq.ctx.Fields) > 0 {
		_spec.Unique = sbq.ctx.Unique != nil && *sbq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, sbq.driver, _spec)
}

func (sbq *SecurityBlacklistQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(securityblacklist.Table, securityblacklist.Columns, sqlgraph.NewFieldSpec(securityblacklist.FieldID, field.TypeString))
	_spec.From = sbq.sql
	if unique := sbq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if sbq.path != nil {
		_spec.Unique = true
	}
	if fields := sbq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, securityblacklist.FieldID)
		for i := range fields {
			if fields[i] != securityblacklist.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := sbq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := sbq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := sbq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := sbq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (sbq *SecurityBlacklistQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(sbq.driver.Dialect())
	t1 := builder.Table(securityblacklist.Table)
	columns := sbq.ctx.Fields
	if len(columns) == 0 {
		columns = securityblacklist.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if sbq.sql != nil {
		selector = sbq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if sbq.ctx.Unique != nil && *sbq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range sbq.predicates {
		p(selector)
	}
	for _, p := range sbq.order {
		p(selector)
	}
	if offset := sbq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := sbq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SecurityBlacklistGroupBy is the group-by builder for SecurityBlacklist entities.
type SecurityBlacklistGroupBy struct {
	selector
	build *SecurityBlacklistQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sbgb *SecurityBlacklistGroupBy) Aggregate(fns ...AggregateFunc) *SecurityBlacklistGroupBy {
	sbgb.fns = append(sbgb.fns, fns...)
	return sbgb
}

// Scan applies the selector query and scans the result into the given value.
func (sbgb *SecurityBlacklistGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sbgb.build.ctx, ent.OpQueryGroupBy)
	if err := sbgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SecurityBlacklistQuery, *SecurityBlacklistGroupBy](ctx, sbgb.build, sbgb, sbgb.build.inters, v)
}

func (sbgb *SecurityBlacklistGroupBy) sqlScan(ctx context.Context, root *SecurityBlacklistQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(sbgb.fns))
	for _, fn := range sbgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*sbgb.flds)+len(sbgb.fns))
		for _, f := range *sbgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*sbgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sbgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SecurityBlacklistSelect is the builder for selecting fields of SecurityBlacklist entities.
type SecurityBlacklistSelect struct {
	*SecurityBlacklistQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sbs *SecurityBlacklistSelect) Aggregate(fns ...AggregateFunc) *SecurityBlacklistSelect {
	sbs.fns = append(sbs.fns, fns...)
	return sbs
}

// Scan applies the selector query and scans the result into the given value.
func (sbs *SecurityBlacklistSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sbs.ctx, ent.OpQuerySelect)
	if err := sbs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SecurityBlacklistQuery, *SecurityBlacklistSelect](ctx, sbs.SecurityBlacklistQuery, sbs, sbs.inters, v)
}

func (sbs *SecurityBlacklistSelect) sqlScan(ctx context.Context, root *SecurityBlacklistQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sbs.fns))
	for _, fn := range sbs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sbs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sbs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/predicate"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/recoveryrequest"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/user"
)

// RecoveryRequestQuery is the builder for querying RecoveryRequest entities.
type RecoveryRequestQuery struct {
	config
	ctx        *QueryContext
	order      []recoveryrequest.OrderOption
	inters     []Interceptor
	predicates []predicate.RecoveryRequest
	withUser   *UserQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RecoveryRequestQuery builder.
func (rrq *RecoveryRequestQuery) Where(ps ...predicate.RecoveryRequest) *RecoveryRequestQuery {
	rrq.predicates = append(rrq.predicates, ps...)
	return rrq
}

// Limit the number of records to be returned by this query.
func (rrq *RecoveryRequestQuery) Limit(limit int) *RecoveryRequestQuery {
	rrq.ctx.Limit = &limit
	return rrq
}

// Offset to start from.
func (rrq *RecoveryRequestQuery) Offset(offset int) *RecoveryRequestQuery {
	rrq.ctx.Offset = &offset
	return rrq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rrq *RecoveryRequestQuery) Unique(unique bool) *RecoveryRequestQuery {
	rrq.ctx.Unique = &unique
	return rrq
}

// Order specifies how the records should be ordered.
func (rrq *RecoveryRequestQuery) Order(o ...recoveryrequest.OrderOption) *RecoveryRequestQuery {
	rrq.order = append(rrq.order, o...)
	return rrq
}

// QueryUser chains the current query on the "user" edge.
func (rrq *RecoveryRequestQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: rrq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rrq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rrq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(recoveryrequest.Table, recoveryrequest.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, recoveryrequest.UserTable, recoveryrequest.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(rrq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first RecoveryRequest entity from the query.
// Returns a *NotFoundError when no RecoveryRequest was found.
func (rrq *RecoveryRequestQuery) First(ctx context.Context) (*RecoveryRequest, error) {
	nodes, err := rrq.Limit(1).All(setContextOp(ctx, rrq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{recoveryrequest.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rrq *RecoveryRequestQuery) FirstX(ctx context.Context) *RecoveryRequest {
	node, err := rrq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RecoveryRequest ID from the query.
// Returns a *NotFoundError when no RecoveryRequest ID was found.
func (rrq *RecoveryRequestQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = rrq.Limit(1).IDs(setContextOp(ctx, rrq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{recoveryrequest.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rrq *RecoveryRequestQuery) FirstIDX(ctx context.Context) string {
	id, err := rrq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RecoveryRequest entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RecoveryRequest entity is found.
// Returns a *NotFoundError when no RecoveryRequest entities are found.
func (rrq *RecoveryRequestQuery) Only(ctx context.Context) (*RecoveryRequest, error) {
	nodes, err := rrq.Limit(2).All(setContextOp(ctx, rrq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{recoveryrequest.Label}
	default:
		return nil, &NotSingularError{recoveryrequest.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rrq *RecoveryRequestQuery) OnlyX(ctx context.Context) *RecoveryRequest {
	node, err := rrq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RecoveryRequest ID in the query.
// Returns a *NotSingularError when more than one RecoveryRequest ID is found.
// Returns a *NotFoundError when no entities are found.
func (rrq *RecoveryRequestQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = rrq.Limit(2).IDs(setContextOp(ctx, rrq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{recoveryrequest.Label}
	default:
		err = &NotSingularError{recoveryrequest.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rrq *RecoveryRequestQuery) OnlyIDX(ctx context.Context) string {
	id, err := rrq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RecoveryRequests.
func (rrq *RecoveryRequestQuery) All(ctx context.Context) ([]*RecoveryRequest, error) {
	ctx = setContextOp(ctx, rrq.ctx, ent.OpQueryAll)
	if err := rrq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RecoveryRequest, *RecoveryRequestQuery]()
	return withInterceptors[[]*RecoveryRequest](ctx, rrq, qr, rrq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rrq *RecoveryRequestQuery) AllX(ctx context.Context) []*RecoveryRequest {
	nodes, err := rrq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RecoveryRequest IDs.
func (rrq *RecoveryRequestQuery) IDs(ctx context.Context) (ids []string, err error) {
	if rrq.ctx.Unique == nil && rrq.path != nil {
		rrq.Unique(true)
	}
	ctx = setContextOp(ctx, rrq.ctx, ent.OpQueryIDs)
	if err = rrq.Select(recoveryrequest.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rrq *RecoveryRequestQuery) IDsX(ctx context.Context) []string {
	ids, err := rrq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rrq *RecoveryRequestQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rrq.ctx, ent.OpQueryCount)
	if err := rrq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rrq, querierCount[*RecoveryRequestQuery](), rrq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rrq *RecoveryRequestQuery) CountX(ctx context.Context) int {
	count, err := rrq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rrq *RecoveryRequestQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rrq.ctx, ent.OpQueryExist)
	switch _, err := rrq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rrq *RecoveryRequestQuery) ExistX(ctx context.Context) bool {
	exist, err := rrq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RecoveryRequestQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rrq *RecoveryRequestQuery) Clone() *RecoveryRequestQuery {
	if rrq == nil {
		return nil
	}
	return &RecoveryRequestQuery{
		config:     rrq.config,
		ctx:        rrq.ctx.Clone(),
		order:      append([]recoveryrequest.OrderOption{}, rrq.order...),
		inters:     append([]Interceptor{}, rrq.inters...),
		predicates: append([]predicate.RecoveryRequest{}, rrq.predicates...),
		withUser:   rrq.withUser.Clone(),
		// clone intermediate query.
		sql:  rrq.sql.Clone(),
		path: rrq.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (rrq *RecoveryRequestQuery) WithUser(opts ...func(*UserQuery)) *RecoveryRequestQuery {
	query := (&UserClient{config: rrq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rrq.withUser = query
	return rrq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID string `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RecoveryRequest.Query().
//		GroupBy(recoveryrequest.FieldUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (rrq *RecoveryRequestQuery) GroupBy(field string, fields ...string) *RecoveryRequestGroupBy {
	rrq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RecoveryRequestGroupBy{build: rrq}
	grbuild.flds = &rrq.ctx.Fields
	grbuild.label = recoveryrequest.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID string `json:"user_id,omitempty"`
//	}
//
//	client.RecoveryRequest.Query().
//		Select(recoveryrequest.FieldUserID).
//		Scan(ctx, &v)
func (rrq *RecoveryRequestQuery) Select(fields ...string) *RecoveryRequestSelect {
	rrq.ctx.Fields = append(rrq.ctx.Fields, fields...)
	sbuild := &RecoveryRequestSelect{RecoveryRequestQuery: rrq}
	sbuild.label = recoveryrequest.Label
	sbuild.flds, sbuild.scan = &rrq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RecoveryRequestSelect configured with the given aggregations.
func (rrq *RecoveryRequestQuery) Aggregate(fns ...AggregateFunc) *RecoveryRequestSelect {
	return rrq.Select().Aggregate(fns...)
}

func (rrq *RecoveryRequestQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rrq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rrq); err != nil {
				return err
			}
		}
	}
	for _, f := range rrq.ctx.Fields {
		if !recoveryrequest.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if rrq.path != nil {
		prev, err := rrq.path(ctx)
		if err != nil {
			return err
		}
		rrq.sql = prev
	}
	return nil
}

func (rrq *RecoveryRequestQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RecoveryRequest, error) {
	var (
		nodes       = []*RecoveryRequest{}
		_spec       = rrq.querySpec()
		loadedTypes = [1]bool{
			rrq.withUser != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RecoveryRequest).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RecoveryRequest{config: rrq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rrq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := rrq.withUser; query != nil {
		if err := rrq.loadUser(ctx, query, nodes, nil,
			func(n *RecoveryRequest, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (rrq *RecoveryRequestQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*RecoveryRequest, init func(*RecoveryRequest), assign func(*RecoveryRequest, *User)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*RecoveryRequest)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (rrq *RecoveryRequestQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rrq.querySpec()
	_spec.Node.Columns = rrq.ctx.Fields
	if len(rrq.ctx.Fields) > 0 {
		_spec.Unique = rrq.ctx.Unique != nil && *rrq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rrq.driver, _spec)
}

func (rrq *RecoveryRequestQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(recoveryrequest.Table, recoveryrequest.Columns, sqlgraph.NewFieldSpec(recoveryrequest.FieldID, field.TypeString))
	_spec.From = rrq.sql
	if unique := rrq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if rrq.path != nil {
		_spec.Unique = true
	}
	if fields := rrq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, recoveryrequest.FieldID)
		for i := range fields {
			if fields[i] != recoveryrequest.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if rrq.withUser != nil {
			_spec.Node.AddColumnOnce(recoveryrequest.FieldUserID)
		}
	}
	if ps := rrq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rrq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rrq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rrq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rrq *RecoveryRequestQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rrq.driver.Dialect())
	t1 := builder.Table(recoveryrequest.Table)
	columns := rrq.ctx.Fields
	if len(columns) == 0 {
		columns = recoveryrequest.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rrq.sql != nil {
		selector = rrq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rrq.ctx.Unique != nil && *rrq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range rrq.predicates {
		p(selector)
	}
	for _, p := range rrq.order {
		p(selector)
	}
	if offset := rrq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rrq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// RecoveryRequestGroupBy is the group-by builder for RecoveryRequest entities.
type RecoveryRequestGroupBy struct {
	selector
	build *RecoveryRequestQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rrgb *RecoveryRequestGroupBy) Aggregate(fns ...AggregateFunc) *RecoveryRequestGroupBy {
	rrgb.fns = append(rrgb.fns, fns...)
	return rrgb
}

// Scan applies the selector query and scans the result into the given value.
func (rrgb *RecoveryRequestGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rrgb.build.ctx, ent.OpQueryGroupBy)
	if err := rrgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RecoveryRequestQuery, *RecoveryRequestGroupBy](ctx, rrgb.build, rrgb, rrgb.build.inters, v)
}

func (rrgb *RecoveryRequestGroupBy) sqlScan(ctx context.Context, root *RecoveryRequestQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rrgb.fns))
	for _, fn := range rrgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rrgb.flds)+len(rrgb.fns))
		for _, f := range *rrgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rrgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rrgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RecoveryRequestSelect is the builder for selecting fields of RecoveryRequest entities.
type RecoveryRequestSelect struct {
	*RecoveryRequestQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rrs *RecoveryRequestSelect) Aggregate(fns ...AggregateFunc) *RecoveryRequestSelect {
	rrs.fns = append(rrs.fns, fns...)
	return rrs
}

// Scan applies the selector query and scans the result into the given value.
func (rrs *RecoveryRequestSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rrs.ctx, ent.OpQuerySelect)
	if err := rrs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RecoveryRequestQuery, *RecoveryRequestSelect](ctx, rrs.RecoveryRequestQuery, rrs, rrs.inters, v)
}

func (rrs *RecoveryRequestSelect) sqlScan(ctx context.Context, root *RecoveryRequestQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rrs.fns))
	for _, fn := range rrs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rrs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rrs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

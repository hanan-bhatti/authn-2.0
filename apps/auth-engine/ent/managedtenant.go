// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/managedtenant"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/tenant"
)

// ManagedTenant is the model entity for the ManagedTenant schema.
type ManagedTenant struct {
	config `json:"-"`
	// ID of the ent.
	// Unique ManagedTenant ID identifier (e.g. mtn_1a2b3c)
	ID string `json:"id,omitempty"`
	// The PLATFORM tenant that owns this ownership record — not the tenant being managed. This is what the privacy interceptor filters on.
	TenantID string `json:"tenant_id,omitempty"`
	// The customer tenant being managed. A plain indexed string with no edge: an edge would generate an eager-load whose query the interceptor scopes to the platform tenant, silently yielding a nil edge instead of the customer tenant. Resolve it through a confined bypass after authorization instead.
	ManagedTenantID string `json:"managed_tenant_id,omitempty"`
	// The platform-tenant user who owns or belongs to the managed tenant. A plain indexed string with no edge, for the same reason as managed_tenant_id.
	OwnerUserID string `json:"owner_user_id,omitempty"`
	// Whether this user owns the managed tenant or merely has access to it
	Role managedtenant.Role `json:"role,omitempty"`
	// Timestamp when the ownership record was created
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ManagedTenantQuery when eager-loading is set.
	Edges        ManagedTenantEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ManagedTenantEdges holds the relations/edges for other nodes in the graph.
type ManagedTenantEdges struct {
	// Tenant holds the value of the tenant edge.
	Tenant *Tenant `json:"tenant,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// TenantOrErr returns the Tenant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ManagedTenantEdges) TenantOrErr() (*Tenant, error) {
	if e.Tenant != nil {
		return e.Tenant, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: tenant.Label}
	}
	return nil, &NotLoadedError{edge: "tenant"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ManagedTenant) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case managedtenant.FieldID, managedtenant.FieldTenantID, managedtenant.FieldManagedTenantID, managedtenant.FieldOwnerUserID, managedtenant.FieldRole:
			values[i] = new(sql.NullString)
		case managedtenant.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ManagedTenant fields.
func (mt *ManagedTenant) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case managedtenant.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				mt.ID = value.String
			}
		case managedtenant.FieldTenantID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value.Valid {
				mt.TenantID = value.String
			}
		case managedtenant.FieldManagedTenantID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field managed_tenant_id", values[i])
			} else if value.Valid {
				mt.ManagedTenantID = value.String
			}
		case managedtenant.FieldOwnerUserID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field owner_user_id", values[i])
			} else if value.Valid {
				mt.OwnerUserID = value.String
			}
		case managedtenant.FieldRole:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field role", values[i])
			} else if value.Valid {
				mt.Role = managedtenant.Role(value.String)
			}
		case managedtenant.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				mt.CreatedAt = value.Time
			}
		default:
			mt.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ManagedTenant.
// This includes values selected through modifiers, order, etc.
func (mt *ManagedTenant) Value(name string) (ent.Value, error) {
	return mt.selectValues.Get(name)
}

// QueryTenant queries the "tenant" edge of the ManagedTenant entity.
func (mt *ManagedTenant) QueryTenant() *TenantQuery {
	return NewManagedTenantClient(mt.config).QueryTenant(mt)
}

// Update returns a builder for updating this ManagedTenant.
// Note that you need to call ManagedTenant.Unwrap() before calling this method if this ManagedTenant
// was returned from a transaction, and the transaction was committed or rolled back.
func (mt *ManagedTenant) Update() *ManagedTenantUpdateOne {
	return NewManagedTenantClient(mt.config).UpdateOne(mt)
}

// Unwrap unwraps the ManagedTenant entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (mt *ManagedTenant) Unwrap() *ManagedTenant {
	_tx, ok := mt.config.driver.(*txDriver)
	if !ok {
		panic("ent: ManagedTenant is not a transactional entity")
	}
	mt.config.driver = _tx.drv
	return mt
}

// String implements the fmt.Stringer.
func (mt *ManagedTenant) String() string {
	var builder strings.Builder
	builder.WriteString("ManagedTenant(")
	builder.WriteString(fmt.Sprintf("id=%v, ", mt.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(mt.TenantID)
	builder.WriteString(", ")
	builder.WriteString("managed_tenant_id=")
	builder.WriteString(mt.ManagedTenantID)
	builder.WriteString(", ")
	builder.WriteString("owner_user_id=")
	builder.WriteString(mt.OwnerUserID)
	builder.WriteString(", ")
	builder.WriteString("role=")
	builder.WriteString(fmt.Sprintf("%v", mt.Role))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(mt.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ManagedTenants is a parsable slice of ManagedTenant.
type ManagedTenants []*ManagedTenant

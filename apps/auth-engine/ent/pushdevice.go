// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/pushdevice"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/user"
)

// PushDevice is the model entity for the PushDevice schema.
type PushDevice struct {
	config `json:"-"`
	// ID of the ent.
	// Unique Push Device ID identifier (e.g. dev_1a2b3c)
	ID string `json:"id,omitempty"`
	// Owning User ID
	UserID string `json:"user_id,omitempty"`
	// Mobile platform: android (FCM) or ios (APNs)
	Platform pushdevice.Platform `json:"platform,omitempty"`
	// Device hardware name (e.g. Google Pixel 8 Pro, iPhone 15 Pro Max)
	DeviceName string `json:"device_name,omitempty"`
	// Operating system version string (e.g. Android 15, iOS 18.2)
	OsVersion string `json:"os_version,omitempty"`
	// FCM registration token or APNs device token string
	DeviceToken string `json:"device_token,omitempty"`
	// PEM-encoded public key generated in mobile KeyStore/Secure Enclave
	PublicKeyPem string `json:"public_key_pem,omitempty"`
	// Derived device location string (e.g. London, UK)
	Location string `json:"location,omitempty"`
	// Device GPS latitude coordinate
	Latitude *float64 `json:"latitude,omitempty"`
	// Device GPS longitude coordinate
	Longitude *float64 `json:"longitude,omitempty"`
	// JSON metadata payload for hardware security telemetry (is_rooted, biometric_type, app_version)
	DeviceMetadata map[string]interface{} `json:"device_metadata,omitempty"`
	// Timestamp when push notification was last sent or approved
	LastUsedAt *time.Time `json:"last_used_at,omitempty"`
	// Registration timestamp
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PushDeviceQuery when eager-loading is set.
	Edges        PushDeviceEdges `json:"edges"`
	selectValues sql.SelectValues
}

// PushDeviceEdges holds the relations/edges for other nodes in the graph.
type PushDeviceEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PushDeviceEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PushDevice) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case pushdevice.FieldDeviceMetadata:
			values[i] = new([]byte)
		case pushdevice.FieldLatitude, pushdevice.FieldLongitude:
			values[i] = new(sql.NullFloat64)
		case pushdevice.FieldID, pushdevice.FieldUserID, pushdevice.FieldPlatform, pushdevice.FieldDeviceName, pushdevice.FieldOsVersion, pushdevice.FieldDeviceToken, pushdevice.FieldPublicKeyPem, pushdevice.FieldLocation:
			values[i] = new(sql.NullString)
		case pushdevice.FieldLastUsedAt, pushdevice.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PushDevice fields.
func (pd *PushDevice) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case pushdevice.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				pd.ID = value.String
			}
		case pushdevice.FieldUserID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				pd.UserID = value.String
			}
		case pushdevice.FieldPlatform:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field platform", values[i])
			} else if value.Valid {
				pd.Platform = pushdevice.Platform(value.String)
			}
		case pushdevice.FieldDeviceName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field device_name", values[i])
			} else if value.Valid {
				pd.DeviceName = value.String
			}
		case pushdevice.FieldOsVersion:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field os_version", values[i])
			} else if value.Valid {
				pd.OsVersion = value.String
			}
		case pushdevice.FieldDeviceToken:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field device_token", values[i])
			} else if value.Valid {
				pd.DeviceToken = value.String
			}
		case pushdevice.FieldPublicKeyPem:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field public_key_pem", values[i])
			} else if value.Valid {
				pd.PublicKeyPem = value.String
			}
		case pushdevice.FieldLocation:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field location", values[i])
			} else if value.Valid {
				pd.Location = value.String
			}
		case pushdevice.FieldLatitude:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field latitude", values[i])
			} else if value.Valid {
				pd.Latitude = new(float64)
				*pd.Latitude = value.Float64
			}
		case pushdevice.FieldLongitude:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field longitude", values[i])
			} else if value.Valid {
				pd.Longitude = new(float64)
				*pd.Longitude = value.Float64
			}
		case pushdevice.FieldDeviceMetadata:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field device_metadata", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &pd.DeviceMetadata); err != nil {
					return fmt.Errorf("unmarshal field device_metadata: %w", err)
				}
			}
		case pushdevice.FieldLastUsedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_used_at", values[i])
			} else if value.Valid {
				pd.LastUsedAt = new(time.Time)
				*pd.LastUsedAt = value.Time
			}
		case pushdevice.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				pd.CreatedAt = value.Time
			}
		default:
			pd.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PushDevice.
// This includes values selected through modifiers, order, etc.
func (pd *PushDevice) Value(name string) (ent.Value, error) {
	return pd.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the PushDevice entity.
func (pd *PushDevice) QueryUser() *UserQuery {
	return NewPushDeviceClient(pd.config).QueryUser(pd)
}

// Update returns a builder for updating this PushDevice.
// Note that you need to call PushDevice.Unwrap() before calling this method if this PushDevice
// was returned from a transaction, and the transaction was committed or rolled back.
func (pd *PushDevice) Update() *PushDeviceUpdateOne {
	return NewPushDeviceClient(pd.config).UpdateOne(pd)
}

// Unwrap unwraps the PushDevice entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pd *PushDevice) Unwrap() *PushDevice {
	_tx, ok := pd.config.driver.(*txDriver)
	if !ok {
		panic("ent: PushDevice is not a transactional entity")
	}
	pd.config.driver = _tx.drv
	return pd
}

// String implements the fmt.Stringer.
func (pd *PushDevice) String() string {
	var builder strings.Builder
	builder.WriteString("PushDevice(")
	builder.WriteString(fmt.Sprintf("id=%v, ", pd.ID))
	builder.WriteString("user_id=")
	builder.WriteString(pd.UserID)
	builder.WriteString(", ")
	builder.WriteString("platform=")
	builder.WriteString(fmt.Sprintf("%v", pd.Platform))
	builder.WriteString(", ")
	builder.WriteString("device_name=")
	builder.WriteString(pd.DeviceName)
	builder.WriteString(", ")
	builder.WriteString("os_version=")
	builder.WriteString(pd.OsVersion)
	builder.WriteString(", ")
	builder.WriteString("device_token=")
	builder.WriteString(pd.DeviceToken)
	builder.WriteString(", ")
	builder.WriteString("public_key_pem=")
	builder.WriteString(pd.PublicKeyPem)
	builder.WriteString(", ")
	builder.WriteString("location=")
	builder.WriteString(pd.Location)
	builder.WriteString(", ")
	if v := pd.Latitude; v != nil {
		builder.WriteString("latitude=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	if v := pd.Longitude; v != nil {
		builder.WriteString("longitude=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("device_metadata=")
	builder.WriteString(fmt.Sprintf("%v", pd.DeviceMetadata))
	builder.WriteString(", ")
	if v := pd.LastUsedAt; v != nil {
		builder.WriteString("last_used_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(pd.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// PushDevices is a parsable slice of PushDevice.
type PushDevices []*PushDevice

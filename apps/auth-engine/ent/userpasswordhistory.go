// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/user"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/userpasswordhistory"
)

// UserPasswordHistory is the model entity for the UserPasswordHistory schema.
type UserPasswordHistory struct {
	config `json:"-"`
	// ID of the ent.
	// Unique Password History ID (e.g. pwh_1a2b3c)
	ID string `json:"id,omitempty"`
	// Owning User ID
	UserID string `json:"user_id,omitempty"`
	// Argon2id password hash of previous password
	PasswordHash string `json:"-"`
	// Timestamp when password was replaced/archived
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the UserPasswordHistoryQuery when eager-loading is set.
	Edges        UserPasswordHistoryEdges `json:"edges"`
	selectValues sql.SelectValues
}

// UserPasswordHistoryEdges holds the relations/edges for other nodes in the graph.
type UserPasswordHistoryEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserPasswordHistoryEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UserPasswordHistory) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case userpasswordhistory.FieldID, userpasswordhistory.FieldUserID, userpasswordhistory.FieldPasswordHash:
			values[i] = new(sql.NullString)
		case userpasswordhistory.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UserPasswordHistory fields.
func (uph *UserPasswordHistory) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case userpasswordhistory.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				uph.ID = value.String
			}
		case userpasswordhistory.FieldUserID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				uph.UserID = value.String
			}
		case userpasswordhistory.FieldPasswordHash:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field password_hash", values[i])
			} else if value.Valid {
				uph.PasswordHash = value.String
			}
		case userpasswordhistory.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				uph.CreatedAt = value.Time
			}
		default:
			uph.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UserPasswordHistory.
// This includes values selected through modifiers, order, etc.
func (uph *UserPasswordHistory) Value(name string) (ent.Value, error) {
	return uph.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the UserPasswordHistory entity.
func (uph *UserPasswordHistory) QueryUser() *UserQuery {
	return NewUserPasswordHistoryClient(uph.config).QueryUser(uph)
}

// Update returns a builder for updating this UserPasswordHistory.
// Note that you need to call UserPasswordHistory.Unwrap() before calling this method if this UserPasswordHistory
// was returned from a transaction, and the transaction was committed or rolled back.
func (uph *UserPasswordHistory) Update() *UserPasswordHistoryUpdateOne {
	return NewUserPasswordHistoryClient(uph.config).UpdateOne(uph)
}

// Unwrap unwraps the UserPasswordHistory entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (uph *UserPasswordHistory) Unwrap() *UserPasswordHistory {
	_tx, ok := uph.config.driver.(*txDriver)
	if !ok {
		panic("ent: UserPasswordHistory is not a transactional entity")
	}
	uph.config.driver = _tx.drv
	return uph
}

// String implements the fmt.Stringer.
func (uph *UserPasswordHistory) String() string {
	var builder strings.Builder
	builder.WriteString("UserPasswordHistory(")
	builder.WriteString(fmt.Sprintf("id=%v, ", uph.ID))
	builder.WriteString("user_id=")
	builder.WriteString(uph.UserID)
	builder.WriteString(", ")
	builder.WriteString("password_hash=<sensitive>")
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(uph.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// UserPasswordHistories is a parsable slice of UserPasswordHistory.
type UserPasswordHistories []*UserPasswordHistory

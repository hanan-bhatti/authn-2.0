// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/organization"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/orginvitation"
)

// OrgInvitation is the model entity for the OrgInvitation schema.
type OrgInvitation struct {
	config `json:"-"`
	// ID of the ent.
	// Unique Invitation ID identifier (e.g. inv_1a2b3c)
	ID string `json:"id,omitempty"`
	// Owning Organization ID
	OrganizationID string `json:"organization_id,omitempty"`
	// Invitee email address
	Email string `json:"email,omitempty"`
	// Assigned Role ID upon invitation acceptance
	RoleID string `json:"role_id,omitempty"`
	// User ID of the admin or team member who sent the invitation
	InvitedByUserID *string `json:"invited_by_user_id,omitempty"`
	// Single-use cryptographically random 32-byte invitation token
	InvitationToken string `json:"invitation_token,omitempty"`
	// Invitation status: pending, accepted, or expired
	Status orginvitation.Status `json:"status,omitempty"`
	// Invitation expiration timestamp (7-day default)
	ExpiresAt time.Time `json:"expires_at,omitempty"`
	// Creation timestamp
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the OrgInvitationQuery when eager-loading is set.
	Edges        OrgInvitationEdges `json:"edges"`
	selectValues sql.SelectValues
}

// OrgInvitationEdges holds the relations/edges for other nodes in the graph.
type OrgInvitationEdges struct {
	// Organization holds the value of the organization edge.
	Organization *Organization `json:"organization,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// OrganizationOrErr returns the Organization value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e OrgInvitationEdges) OrganizationOrErr() (*Organization, error) {
	if e.Organization != nil {
		return e.Organization, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: organization.Label}
	}
	return nil, &NotLoadedError{edge: "organization"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*OrgInvitation) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case orginvitation.FieldID, orginvitation.FieldOrganizationID, orginvitation.FieldEmail, orginvitation.FieldRoleID, orginvitation.FieldInvitedByUserID, orginvitation.FieldInvitationToken, orginvitation.FieldStatus:
			values[i] = new(sql.NullString)
		case orginvitation.FieldExpiresAt, orginvitation.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the OrgInvitation fields.
func (oi *OrgInvitation) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case orginvitation.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				oi.ID = value.String
			}
		case orginvitation.FieldOrganizationID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field organization_id", values[i])
			} else if value.Valid {
				oi.OrganizationID = value.String
			}
		case orginvitation.FieldEmail:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field email", values[i])
			} else if value.Valid {
				oi.Email = value.String
			}
		case orginvitation.FieldRoleID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field role_id", values[i])
			} else if value.Valid {
				oi.RoleID = value.String
			}
		case orginvitation.FieldInvitedByUserID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field invited_by_user_id", values[i])
			} else if value.Valid {
				oi.InvitedByUserID = new(string)
				*oi.InvitedByUserID = value.String
			}
		case orginvitation.FieldInvitationToken:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field invitation_token", values[i])
			} else if value.Valid {
				oi.InvitationToken = value.String
			}
		case orginvitation.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				oi.Status = orginvitation.Status(value.String)
			}
		case orginvitation.FieldExpiresAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field expires_at", values[i])
			} else if value.Valid {
				oi.ExpiresAt = value.Time
			}
		case orginvitation.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				oi.CreatedAt = value.Time
			}
		default:
			oi.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the OrgInvitation.
// This includes values selected through modifiers, order, etc.
func (oi *OrgInvitation) Value(name string) (ent.Value, error) {
	return oi.selectValues.Get(name)
}

// QueryOrganization queries the "organization" edge of the OrgInvitation entity.
func (oi *OrgInvitation) QueryOrganization() *OrganizationQuery {
	return NewOrgInvitationClient(oi.config).QueryOrganization(oi)
}

// Update returns a builder for updating this OrgInvitation.
// Note that you need to call OrgInvitation.Unwrap() before calling this method if this OrgInvitation
// was returned from a transaction, and the transaction was committed or rolled back.
func (oi *OrgInvitation) Update() *OrgInvitationUpdateOne {
	return NewOrgInvitationClient(oi.config).UpdateOne(oi)
}

// Unwrap unwraps the OrgInvitation entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (oi *OrgInvitation) Unwrap() *OrgInvitation {
	_tx, ok := oi.config.driver.(*txDriver)
	if !ok {
		panic("ent: OrgInvitation is not a transactional entity")
	}
	oi.config.driver = _tx.drv
	return oi
}

// String implements the fmt.Stringer.
func (oi *OrgInvitation) String() string {
	var builder strings.Builder
	builder.WriteString("OrgInvitation(")
	builder.WriteString(fmt.Sprintf("id=%v, ", oi.ID))
	builder.WriteString("organization_id=")
	builder.WriteString(oi.OrganizationID)
	builder.WriteString(", ")
	builder.WriteString("email=")
	builder.WriteString(oi.Email)
	builder.WriteString(", ")
	builder.WriteString("role_id=")
	builder.WriteString(oi.RoleID)
	builder.WriteString(", ")
	if v := oi.InvitedByUserID; v != nil {
		builder.WriteString("invited_by_user_id=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("invitation_token=")
	builder.WriteString(oi.InvitationToken)
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", oi.Status))
	builder.WriteString(", ")
	builder.WriteString("expires_at=")
	builder.WriteString(oi.ExpiresAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(oi.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// OrgInvitations is a parsable slice of OrgInvitation.
type OrgInvitations []*OrgInvitation

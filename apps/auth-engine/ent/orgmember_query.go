// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/organization"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/orgmember"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/predicate"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/role"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/user"
)

// OrgMemberQuery is the builder for querying OrgMember entities.
type OrgMemberQuery struct {
	config
	ctx              *QueryContext
	order            []orgmember.OrderOption
	inters           []Interceptor
	predicates       []predicate.OrgMember
	withOrganization *OrganizationQuery
	withUser         *UserQuery
	withRole         *RoleQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the OrgMemberQuery builder.
func (omq *OrgMemberQuery) Where(ps ...predicate.OrgMember) *OrgMemberQuery {
	omq.predicates = append(omq.predicates, ps...)
	return omq
}

// Limit the number of records to be returned by this query.
func (omq *OrgMemberQuery) Limit(limit int) *OrgMemberQuery {
	omq.ctx.Limit = &limit
	return omq
}

// Offset to start from.
func (omq *OrgMemberQuery) Offset(offset int) *OrgMemberQuery {
	omq.ctx.Offset = &offset
	return omq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (omq *OrgMemberQuery) Unique(unique bool) *OrgMemberQuery {
	omq.ctx.Unique = &unique
	return omq
}

// Order specifies how the records should be ordered.
func (omq *OrgMemberQuery) Order(o ...orgmember.OrderOption) *OrgMemberQuery {
	omq.order = append(omq.order, o...)
	return omq
}

// QueryOrganization chains the current query on the "organization" edge.
func (omq *OrgMemberQuery) QueryOrganization() *OrganizationQuery {
	query := (&OrganizationClient{config: omq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := omq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := omq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(orgmember.Table, orgmember.FieldID, selector),
			sqlgraph.To(organization.Table, organization.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, orgmember.OrganizationTable, orgmember.OrganizationColumn),
		)
		fromU = sqlgraph.SetNeighbors(omq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUser chains the current query on the "user" edge.
func (omq *OrgMemberQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: omq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := omq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := omq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(orgmember.Table, orgmember.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, orgmember.UserTable, orgmember.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(omq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRole chains the current query on the "role" edge.
func (omq *OrgMemberQuery) QueryRole() *RoleQuery {
	query := (&RoleClient{config: omq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := omq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := omq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(orgmember.Table, orgmember.FieldID, selector),
			sqlgraph.To(role.Table, role.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, orgmember.RoleTable, orgmember.RoleColumn),
		)
		fromU = sqlgraph.SetNeighbors(omq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first OrgMember entity from the query.
// Returns a *NotFoundError when no OrgMember was found.
func (omq *OrgMemberQuery) First(ctx context.Context) (*OrgMember, error) {
	nodes, err := omq.Limit(1).All(setContextOp(ctx, omq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{orgmember.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (omq *OrgMemberQuery) FirstX(ctx context.Context) *OrgMember {
	node, err := omq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first OrgMember ID from the query.
// Returns a *NotFoundError when no OrgMember ID was found.
func (omq *OrgMemberQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = omq.Limit(1).IDs(setContextOp(ctx, omq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{orgmember.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (omq *OrgMemberQuery) FirstIDX(ctx context.Context) string {
	id, err := omq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single OrgMember entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one OrgMember entity is found.
// Returns a *NotFoundError when no OrgMember entities are found.
func (omq *OrgMemberQuery) Only(ctx context.Context) (*OrgMember, error) {
	nodes, err := omq.Limit(2).All(setContextOp(ctx, omq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{orgmember.Label}
	default:
		return nil, &NotSingularError{orgmember.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (omq *OrgMemberQuery) OnlyX(ctx context.Context) *OrgMember {
	node, err := omq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only OrgMember ID in the query.
// Returns a *NotSingularError when more than one OrgMember ID is found.
// Returns a *NotFoundError when no entities are found.
func (omq *OrgMemberQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = omq.Limit(2).IDs(setContextOp(ctx, omq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{orgmember.Label}
	default:
		err = &NotSingularError{orgmember.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (omq *OrgMemberQuery) OnlyIDX(ctx context.Context) string {
	id, err := omq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of OrgMembers.
func (omq *OrgMemberQuery) All(ctx context.Context) ([]*OrgMember, error) {
	ctx = setContextOp(ctx, omq.ctx, ent.OpQueryAll)
	if err := omq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*OrgMember, *OrgMemberQuery]()
	return withInterceptors[[]*OrgMember](ctx, omq, qr, omq.inters)
}

// AllX is like All, but panics if an error occurs.
func (omq *OrgMemberQuery) AllX(ctx context.Context) []*OrgMember {
	nodes, err := omq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of OrgMember IDs.
func (omq *OrgMemberQuery) IDs(ctx context.Context) (ids []string, err error) {
	if omq.ctx.Unique == nil && omq.path != nil {
		omq.Unique(true)
	}
	ctx = setContextOp(ctx, omq.ctx, ent.OpQueryIDs)
	if err = omq.Select(orgmember.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (omq *OrgMemberQuery) IDsX(ctx context.Context) []string {
	ids, err := omq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (omq *OrgMemberQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, omq.ctx, ent.OpQueryCount)
	if err := omq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, omq, querierCount[*OrgMemberQuery](), omq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (omq *OrgMemberQuery) CountX(ctx context.Context) int {
	count, err := omq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (omq *OrgMemberQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, omq.ctx, ent.OpQueryExist)
	switch _, err := omq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (omq *OrgMemberQuery) ExistX(ctx context.Context) bool {
	exist, err := omq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the OrgMemberQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (omq *OrgMemberQuery) Clone() *OrgMemberQuery {
	if omq == nil {
		return nil
	}
	return &OrgMemberQuery{
		config:           omq.config,
		ctx:              omq.ctx.Clone(),
		order:            append([]orgmember.OrderOption{}, omq.order...),
		inters:           append([]Interceptor{}, omq.inters...),
		predicates:       append([]predicate.OrgMember{}, omq.predicates...),
		withOrganization: omq.withOrganization.Clone(),
		withUser:         omq.withUser.Clone(),
		withRole:         omq.withRole.Clone(),
		// clone intermediate query.
		sql:  omq.sql.Clone(),
		path: omq.path,
	}
}

// WithOrganization tells the query-builder to eager-load the nodes that are connected to
// the "organization" edge. The optional arguments are used to configure the query builder of the edge.
func (omq *OrgMemberQuery) WithOrganization(opts ...func(*OrganizationQuery)) *OrgMemberQuery {
	query := (&OrganizationClient{config: omq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	omq.withOrganization = query
	return omq
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (omq *OrgMemberQuery) WithUser(opts ...func(*UserQuery)) *OrgMemberQuery {
	query := (&UserClient{config: omq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	omq.withUser = query
	return omq
}

// WithRole tells the query-builder to eager-load the nodes that are connected to
// the "role" edge. The optional arguments are used to configure the query builder of the edge.
func (omq *OrgMemberQuery) WithRole(opts ...func(*RoleQuery)) *OrgMemberQuery {
	query := (&RoleClient{config: omq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	omq.withRole = query
	return omq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		OrganizationID string `json:"organization_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.OrgMember.Query().
//		GroupBy(orgmember.FieldOrganizationID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (omq *OrgMemberQuery) GroupBy(field string, fields ...string) *OrgMemberGroupBy {
	omq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &OrgMemberGroupBy{build: omq}
	grbuild.flds = &omq.ctx.Fields
	grbuild.label = orgmember.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		OrganizationID string `json:"organization_id,omitempty"`
//	}
//
//	client.OrgMember.Query().
//		Select(orgmember.FieldOrganizationID).
//		Scan(ctx, &v)
func (omq *OrgMemberQuery) Select(fields ...string) *OrgMemberSelect {
	omq.ctx.Fields = append(omq.ctx.Fields, fields...)
	sbuild := &OrgMemberSelect{OrgMemberQuery: omq}
	sbuild.label = orgmember.Label
	sbuild.flds, sbuild.scan = &omq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a OrgMemberSelect configured with the given aggregations.
func (omq *OrgMemberQuery) Aggregate(fns ...AggregateFunc) *OrgMemberSelect {
	return omq.Select().Aggregate(fns...)
}

func (omq *OrgMemberQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range omq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, omq); err != nil {
				return err
			}
		}
	}
	for _, f := range omq.ctx.Fields {
		if !orgmember.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if omq.path != nil {
		prev, err := omq.path(ctx)
		if err != nil {
			return err
		}
		omq.sql = prev
	}
	return nil
}

func (omq *OrgMemberQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*OrgMember, error) {
	var (
		nodes       = []*OrgMember{}
		_spec       = omq.querySpec()
		loadedTypes = [3]bool{
			omq.withOrganization != nil,
			omq.withUser != nil,
			omq.withRole != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*OrgMember).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &OrgMember{config: omq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, omq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := omq.withOrganization; query != nil {
		if err := omq.loadOrganization(ctx, query, nodes, nil,
			func(n *OrgMember, e *Organization) { n.Edges.Organization = e }); err != nil {
			return nil, err
		}
	}
	if query := omq.withUser; query != nil {
		if err := omq.loadUser(ctx, query, nodes, nil,
			func(n *OrgMember, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	if query := omq.withRole; query != nil {
		if err := omq.loadRole(ctx, query, nodes, nil,
			func(n *OrgMember, e *Role) { n.Edges.Role = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (omq *OrgMemberQuery) loadOrganization(ctx context.Context, query *OrganizationQuery, nodes []*OrgMember, init func(*OrgMember), assign func(*OrgMember, *Organization)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*OrgMember)
	for i := range nodes {
		fk := nodes[i].OrganizationID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(organization.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "organization_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (omq *OrgMemberQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*OrgMember, init func(*OrgMember), assign func(*OrgMember, *User)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*OrgMember)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (omq *OrgMemberQuery) loadRole(ctx context.Context, query *RoleQuery, nodes []*OrgMember, init func(*OrgMember), assign func(*OrgMember, *Role)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*OrgMember)
	for i := range nodes {
		fk := nodes[i].RoleID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(role.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "role_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (omq *OrgMemberQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := omq.querySpec()
	_spec.Node.Columns = omq.ctx.Fields
	if len(omq.ctx.Fields) > 0 {
		_spec.Unique = omq.ctx.Unique != nil && *omq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, omq.driver, _spec)
}

func (omq *OrgMemberQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(orgmember.Table, orgmember.Columns, sqlgraph.NewFieldSpec(orgmember.FieldID, field.TypeString))
	_spec.From = omq.sql
	if unique := omq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if omq.path != nil {
		_spec.Unique = true
	}
	if fields := omq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, orgmember.FieldID)
		for i := range fields {
			if fields[i] != orgmember.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if omq.withOrganization != nil {
			_spec.Node.AddColumnOnce(orgmember.FieldOrganizationID)
		}
		if omq.withUser != nil {
			_spec.Node.AddColumnOnce(orgmember.FieldUserID)
		}
		if omq.withRole != nil {
			_spec.Node.AddColumnOnce(orgmember.FieldRoleID)
		}
	}
	if ps := omq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := omq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := omq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := omq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (omq *OrgMemberQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(omq.driver.Dialect())
	t1 := builder.Table(orgmember.Table)
	columns := omq.ctx.Fields
	if len(columns) == 0 {
		columns = orgmember.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if omq.sql != nil {
		selector = omq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if omq.ctx.Unique != nil && *omq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range omq.predicates {
		p(selector)
	}
	for _, p := range omq.order {
		p(selector)
	}
	if offset := omq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := omq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// OrgMemberGroupBy is the group-by builder for OrgMember entities.
type OrgMemberGroupBy struct {
	selector
	build *OrgMemberQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (omgb *OrgMemberGroupBy) Aggregate(fns ...AggregateFunc) *OrgMemberGroupBy {
	omgb.fns = append(omgb.fns, fns...)
	return omgb
}

// Scan applies the selector query and scans the result into the given value.
func (omgb *OrgMemberGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, omgb.build.ctx, ent.OpQueryGroupBy)
	if err := omgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OrgMemberQuery, *OrgMemberGroupBy](ctx, omgb.build, omgb, omgb.build.inters, v)
}

func (omgb *OrgMemberGroupBy) sqlScan(ctx context.Context, root *OrgMemberQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(omgb.fns))
	for _, fn := range omgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*omgb.flds)+len(omgb.fns))
		for _, f := range *omgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*omgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := omgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// OrgMemberSelect is the builder for selecting fields of OrgMember entities.
type OrgMemberSelect struct {
	*OrgMemberQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (oms *OrgMemberSelect) Aggregate(fns ...AggregateFunc) *OrgMemberSelect {
	oms.fns = append(oms.fns, fns...)
	return oms
}

// Scan applies the selector query and scans the result into the given value.
func (oms *OrgMemberSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, oms.ctx, ent.OpQuerySelect)
	if err := oms.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OrgMemberQuery, *OrgMemberSelect](ctx, oms.OrgMemberQuery, oms, oms.inters, v)
}

func (oms *OrgMemberSelect) sqlScan(ctx context.Context, root *OrgMemberQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(oms.fns))
	for _, fn := range oms.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*oms.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := oms.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/predicate"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/socialauthstate"
)

// SocialAuthStateQuery is the builder for querying SocialAuthState entities.
type SocialAuthStateQuery struct {
	config
	ctx        *QueryContext
	order      []socialauthstate.OrderOption
	inters     []Interceptor
	predicates []predicate.SocialAuthState
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SocialAuthStateQuery builder.
func (sasq *SocialAuthStateQuery) Where(ps ...predicate.SocialAuthState) *SocialAuthStateQuery {
	sasq.predicates = append(sasq.predicates, ps...)
	return sasq
}

// Limit the number of records to be returned by this query.
func (sasq *SocialAuthStateQuery) Limit(limit int) *SocialAuthStateQuery {
	sasq.ctx.Limit = &limit
	return sasq
}

// Offset to start from.
func (sasq *SocialAuthStateQuery) Offset(offset int) *SocialAuthStateQuery {
	sasq.ctx.Offset = &offset
	return sasq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (sasq *SocialAuthStateQuery) Unique(unique bool) *SocialAuthStateQuery {
	sasq.ctx.Unique = &unique
	return sasq
}

// Order specifies how the records should be ordered.
func (sasq *SocialAuthStateQuery) Order(o ...socialauthstate.OrderOption) *SocialAuthStateQuery {
	sasq.order = append(sasq.order, o...)
	return sasq
}

// First returns the first SocialAuthState entity from the query.
// Returns a *NotFoundError when no SocialAuthState was found.
func (sasq *SocialAuthStateQuery) First(ctx context.Context) (*SocialAuthState, error) {
	nodes, err := sasq.Limit(1).All(setContextOp(ctx, sasq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{socialauthstate.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (sasq *SocialAuthStateQuery) FirstX(ctx context.Context) *SocialAuthState {
	node, err := sasq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SocialAuthState ID from the query.
// Returns a *NotFoundError when no SocialAuthState ID was found.
func (sasq *SocialAuthStateQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = sasq.Limit(1).IDs(setContextOp(ctx, sasq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{socialauthstate.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (sasq *SocialAuthStateQuery) FirstIDX(ctx context.Context) string {
	id, err := sasq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SocialAuthState entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SocialAuthState entity is found.
// Returns a *NotFoundError when no SocialAuthState entities are found.
func (sasq *SocialAuthStateQuery) Only(ctx context.Context) (*SocialAuthState, error) {
	nodes, err := sasq.Limit(2).All(setContextOp(ctx, sasq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{socialauthstate.Label}
	default:
		return nil, &NotSingularError{socialauthstate.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (sasq *SocialAuthStateQuery) OnlyX(ctx context.Context) *SocialAuthState {
	node, err := sasq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SocialAuthState ID in the query.
// Returns a *NotSingularError when more than one SocialAuthState ID is found.
// Returns a *NotFoundError when no entities are found.
func (sasq *SocialAuthStateQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = sasq.Limit(2).IDs(setContextOp(ctx, sasq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{socialauthstate.Label}
	default:
		err = &NotSingularError{socialauthstate.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (sasq *SocialAuthStateQuery) OnlyIDX(ctx context.Context) string {
	id, err := sasq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SocialAuthStates.
func (sasq *SocialAuthStateQuery) All(ctx context.Context) ([]*SocialAuthState, error) {
	ctx = setContextOp(ctx, sasq.ctx, ent.OpQueryAll)
	if err := sasq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SocialAuthState, *SocialAuthStateQuery]()
	return withInterceptors[[]*SocialAuthState](ctx, sasq, qr, sasq.inters)
}

// AllX is like All, but panics if an error occurs.
func (sasq *SocialAuthStateQuery) AllX(ctx context.Context) []*SocialAuthState {
	nodes, err := sasq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SocialAuthState IDs.
func (sasq *SocialAuthStateQuery) IDs(ctx context.Context) (ids []string, err error) {
	if sasq.ctx.Unique == nil && sasq.path != nil {
		sasq.Unique(true)
	}
	ctx = setContextOp(ctx, sasq.ctx, ent.OpQueryIDs)
	if err = sasq.Select(socialauthstate.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (sasq *SocialAuthStateQuery) IDsX(ctx context.Context) []string {
	ids, err := sasq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (sasq *SocialAuthStateQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, sasq.ctx, ent.OpQueryCount)
	if err := sasq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, sasq, querierCount[*SocialAuthStateQuery](), sasq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (sasq *SocialAuthStateQuery) CountX(ctx context.Context) int {
	count, err := sasq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (sasq *SocialAuthStateQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, sasq.ctx, ent.OpQueryExist)
	switch _, err := sasq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (sasq *SocialAuthStateQuery) ExistX(ctx context.Context) bool {
	exist, err := sasq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SocialAuthStateQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (sasq *SocialAuthStateQuery) Clone() *SocialAuthStateQuery {
	if sasq == nil {
		return nil
	}
	return &SocialAuthStateQuery{
		config:     sasq.config,
		ctx:        sasq.ctx.Clone(),
		order:      append([]socialauthstate.OrderOption{}, sasq.order...),
		inters:     append([]Interceptor{}, sasq.inters...),
		predicates: append([]predicate.SocialAuthState{}, sasq.predicates...),
		// clone intermediate query.
		sql:  sasq.sql.Clone(),
		path: sasq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID string `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SocialAuthState.Query().
//		GroupBy(socialauthstate.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (sasq *SocialAuthStateQuery) GroupBy(field string, fields ...string) *SocialAuthStateGroupBy {
	sasq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SocialAuthStateGroupBy{build: sasq}
	grbuild.flds = &sasq.ctx.Fields
	grbuild.label = socialauthstate.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID string `json:"tenant_id,omitempty"`
//	}
//
//	client.SocialAuthState.Query().
//		Select(socialauthstate.FieldTenantID).
//		Scan(ctx, &v)
func (sasq *SocialAuthStateQuery) Select(fields ...string) *SocialAuthStateSelect {
	sasq.ctx.Fields = append(sasq.ctx.Fields, fields...)
	sbuild := &SocialAuthStateSelect{SocialAuthStateQuery: sasq}
	sbuild.label = socialauthstate.Label
	sbuild.flds, sbuild.scan = &sasq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SocialAuthStateSelect configured with the given aggregations.
func (sasq *SocialAuthStateQuery) Aggregate(fns ...AggregateFunc) *SocialAuthStateSelect {
	return sasq.Select().Aggregate(fns...)
}

func (sasq *SocialAuthStateQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range sasq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, sasq); err != nil {
				return err
			}
		}
	}
	for _, f := range sasq.ctx.Fields {
		if !socialauthstate.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if sasq.path != nil {
		prev, err := sasq.path(ctx)
		if err != nil {
			return err
		}
		sasq.sql = prev
	}
	return nil
}

func (sasq *SocialAuthStateQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SocialAuthState, error) {
	var (
		nodes = []*SocialAuthState{}
		_spec = sasq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SocialAuthState).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SocialAuthState{config: sasq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, sasq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (sasq *SocialAuthStateQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := sasq.querySpec()
	_spec.Node.Columns = sasq.ctx.Fields
	if len(sasq.ctx.Fields) > 0 {
		_spec.Unique = sasq.ctx.Unique != nil && *sasq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, sasq.driver, _spec)
}

func (sasq *SocialAuthStateQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(socialauthstate.Table, socialauthstate.Columns, sqlgraph.NewFieldSpec(socialauthstate.FieldID, field.TypeString))
	_spec.From = sasq.sql
	if unique := sasq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if sasq.path != nil {
		_spec.Unique = true
	}
	if fields := sasq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, socialauthstate.FieldID)
		for i := range fields {
			if fields[i] != socialauthstate.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := sasq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := sasq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := sasq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := sasq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (sasq *SocialAuthStateQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(sasq.driver.Dialect())
	t1 := builder.Table(socialauthstate.Table)
	columns := sasq.ctx.Fields
	if len(columns) == 0 {
		columns = socialauthstate.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if sasq.sql != nil {
		selector = sasq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if sasq.ctx.Unique != nil && *sasq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range sasq.predicates {
		p(selector)
	}
	for _, p := range sasq.order {
		p(selector)
	}
	if offset := sasq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := sasq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SocialAuthStateGroupBy is the group-by builder for SocialAuthState entities.
type SocialAuthStateGroupBy struct {
	selector
	build *SocialAuthStateQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sasgb *SocialAuthStateGroupBy) Aggregate(fns ...AggregateFunc) *SocialAuthStateGroupBy {
	sasgb.fns = append(sasgb.fns, fns...)
	return sasgb
}

// Scan applies the selector query and scans the result into the given value.
func (sasgb *SocialAuthStateGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sasgb.build.ctx, ent.OpQueryGroupBy)
	if err := sasgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SocialAuthStateQuery, *SocialAuthStateGroupBy](ctx, sasgb.build, sasgb, sasgb.build.inters, v)
}

func (sasgb *SocialAuthStateGroupBy) sqlScan(ctx context.Context, root *SocialAuthStateQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(sasgb.fns))
	for _, fn := range sasgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*sasgb.flds)+len(sasgb.fns))
		for _, f := range *sasgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*sasgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sasgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SocialAuthStateSelect is the builder for selecting fields of SocialAuthState entities.
type SocialAuthStateSelect struct {
	*SocialAuthStateQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sass *SocialAuthStateSelect) Aggregate(fns ...AggregateFunc) *SocialAuthStateSelect {
	sass.fns = append(sass.fns, fns...)
	return sass
}

// Scan applies the selector query and scans the result into the given value.
func (sass *SocialAuthStateSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sass.ctx, ent.OpQuerySelect)
	if err := sass.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SocialAuthStateQuery, *SocialAuthStateSelect](ctx, sass.SocialAuthStateQuery, sass, sass.inters, v)
}

func (sass *SocialAuthStateSelect) sqlScan(ctx context.Context, root *SocialAuthStateQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sass.fns))
	for _, fn := range sass.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sass.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sass.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

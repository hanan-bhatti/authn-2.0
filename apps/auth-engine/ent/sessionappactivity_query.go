// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/application"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/predicate"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/session"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/sessionappactivity"
)

// SessionAppActivityQuery is the builder for querying SessionAppActivity entities.
type SessionAppActivityQuery struct {
	config
	ctx             *QueryContext
	order           []sessionappactivity.OrderOption
	inters          []Interceptor
	predicates      []predicate.SessionAppActivity
	withSession     *SessionQuery
	withApplication *ApplicationQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SessionAppActivityQuery builder.
func (saaq *SessionAppActivityQuery) Where(ps ...predicate.SessionAppActivity) *SessionAppActivityQuery {
	saaq.predicates = append(saaq.predicates, ps...)
	return saaq
}

// Limit the number of records to be returned by this query.
func (saaq *SessionAppActivityQuery) Limit(limit int) *SessionAppActivityQuery {
	saaq.ctx.Limit = &limit
	return saaq
}

// Offset to start from.
func (saaq *SessionAppActivityQuery) Offset(offset int) *SessionAppActivityQuery {
	saaq.ctx.Offset = &offset
	return saaq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (saaq *SessionAppActivityQuery) Unique(unique bool) *SessionAppActivityQuery {
	saaq.ctx.Unique = &unique
	return saaq
}

// Order specifies how the records should be ordered.
func (saaq *SessionAppActivityQuery) Order(o ...sessionappactivity.OrderOption) *SessionAppActivityQuery {
	saaq.order = append(saaq.order, o...)
	return saaq
}

// QuerySession chains the current query on the "session" edge.
func (saaq *SessionAppActivityQuery) QuerySession() *SessionQuery {
	query := (&SessionClient{config: saaq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := saaq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := saaq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(sessionappactivity.Table, sessionappactivity.FieldID, selector),
			sqlgraph.To(session.Table, session.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, sessionappactivity.SessionTable, sessionappactivity.SessionColumn),
		)
		fromU = sqlgraph.SetNeighbors(saaq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryApplication chains the current query on the "application" edge.
func (saaq *SessionAppActivityQuery) QueryApplication() *ApplicationQuery {
	query := (&ApplicationClient{config: saaq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := saaq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := saaq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(sessionappactivity.Table, sessionappactivity.FieldID, selector),
			sqlgraph.To(application.Table, application.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, sessionappactivity.ApplicationTable, sessionappactivity.ApplicationColumn),
		)
		fromU = sqlgraph.SetNeighbors(saaq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first SessionAppActivity entity from the query.
// Returns a *NotFoundError when no SessionAppActivity was found.
func (saaq *SessionAppActivityQuery) First(ctx context.Context) (*SessionAppActivity, error) {
	nodes, err := saaq.Limit(1).All(setContextOp(ctx, saaq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{sessionappactivity.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (saaq *SessionAppActivityQuery) FirstX(ctx context.Context) *SessionAppActivity {
	node, err := saaq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SessionAppActivity ID from the query.
// Returns a *NotFoundError when no SessionAppActivity ID was found.
func (saaq *SessionAppActivityQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = saaq.Limit(1).IDs(setContextOp(ctx, saaq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{sessionappactivity.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (saaq *SessionAppActivityQuery) FirstIDX(ctx context.Context) string {
	id, err := saaq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SessionAppActivity entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SessionAppActivity entity is found.
// Returns a *NotFoundError when no SessionAppActivity entities are found.
func (saaq *SessionAppActivityQuery) Only(ctx context.Context) (*SessionAppActivity, error) {
	nodes, err := saaq.Limit(2).All(setContextOp(ctx, saaq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{sessionappactivity.Label}
	default:
		return nil, &NotSingularError{sessionappactivity.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (saaq *SessionAppActivityQuery) OnlyX(ctx context.Context) *SessionAppActivity {
	node, err := saaq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SessionAppActivity ID in the query.
// Returns a *NotSingularError when more than one SessionAppActivity ID is found.
// Returns a *NotFoundError when no entities are found.
func (saaq *SessionAppActivityQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = saaq.Limit(2).IDs(setContextOp(ctx, saaq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{sessionappactivity.Label}
	default:
		err = &NotSingularError{sessionappactivity.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (saaq *SessionAppActivityQuery) OnlyIDX(ctx context.Context) string {
	id, err := saaq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SessionAppActivities.
func (saaq *SessionAppActivityQuery) All(ctx context.Context) ([]*SessionAppActivity, error) {
	ctx = setContextOp(ctx, saaq.ctx, ent.OpQueryAll)
	if err := saaq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SessionAppActivity, *SessionAppActivityQuery]()
	return withInterceptors[[]*SessionAppActivity](ctx, saaq, qr, saaq.inters)
}

// AllX is like All, but panics if an error occurs.
func (saaq *SessionAppActivityQuery) AllX(ctx context.Context) []*SessionAppActivity {
	nodes, err := saaq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SessionAppActivity IDs.
func (saaq *SessionAppActivityQuery) IDs(ctx context.Context) (ids []string, err error) {
	if saaq.ctx.Unique == nil && saaq.path != nil {
		saaq.Unique(true)
	}
	ctx = setContextOp(ctx, saaq.ctx, ent.OpQueryIDs)
	if err = saaq.Select(sessionappactivity.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (saaq *SessionAppActivityQuery) IDsX(ctx context.Context) []string {
	ids, err := saaq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (saaq *SessionAppActivityQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, saaq.ctx, ent.OpQueryCount)
	if err := saaq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, saaq, querierCount[*SessionAppActivityQuery](), saaq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (saaq *SessionAppActivityQuery) CountX(ctx context.Context) int {
	count, err := saaq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (saaq *SessionAppActivityQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, saaq.ctx, ent.OpQueryExist)
	switch _, err := saaq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (saaq *SessionAppActivityQuery) ExistX(ctx context.Context) bool {
	exist, err := saaq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SessionAppActivityQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (saaq *SessionAppActivityQuery) Clone() *SessionAppActivityQuery {
	if saaq == nil {
		return nil
	}
	return &SessionAppActivityQuery{
		config:          saaq.config,
		ctx:             saaq.ctx.Clone(),
		order:           append([]sessionappactivity.OrderOption{}, saaq.order...),
		inters:          append([]Interceptor{}, saaq.inters...),
		predicates:      append([]predicate.SessionAppActivity{}, saaq.predicates...),
		withSession:     saaq.withSession.Clone(),
		withApplication: saaq.withApplication.Clone(),
		// clone intermediate query.
		sql:  saaq.sql.Clone(),
		path: saaq.path,
	}
}

// WithSession tells the query-builder to eager-load the nodes that are connected to
// the "session" edge. The optional arguments are used to configure the query builder of the edge.
func (saaq *SessionAppActivityQuery) WithSession(opts ...func(*SessionQuery)) *SessionAppActivityQuery {
	query := (&SessionClient{config: saaq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	saaq.withSession = query
	return saaq
}

// WithApplication tells the query-builder to eager-load the nodes that are connected to
// the "application" edge. The optional arguments are used to configure the query builder of the edge.
func (saaq *SessionAppActivityQuery) WithApplication(opts ...func(*ApplicationQuery)) *SessionAppActivityQuery {
	query := (&ApplicationClient{config: saaq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	saaq.withApplication = query
	return saaq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		SessionID string `json:"session_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SessionAppActivity.Query().
//		GroupBy(sessionappactivity.FieldSessionID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (saaq *SessionAppActivityQuery) GroupBy(field string, fields ...string) *SessionAppActivityGroupBy {
	saaq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SessionAppActivityGroupBy{build: saaq}
	grbuild.flds = &saaq.ctx.Fields
	grbuild.label = sessionappactivity.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		SessionID string `json:"session_id,omitempty"`
//	}
//
//	client.SessionAppActivity.Query().
//		Select(sessionappactivity.FieldSessionID).
//		Scan(ctx, &v)
func (saaq *SessionAppActivityQuery) Select(fields ...string) *SessionAppActivitySelect {
	saaq.ctx.Fields = append(saaq.ctx.Fields, fields...)
	sbuild := &SessionAppActivitySelect{SessionAppActivityQuery: saaq}
	sbuild.label = sessionappactivity.Label
	sbuild.flds, sbuild.scan = &saaq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SessionAppActivitySelect configured with the given aggregations.
func (saaq *SessionAppActivityQuery) Aggregate(fns ...AggregateFunc) *SessionAppActivitySelect {
	return saaq.Select().Aggregate(fns...)
}

func (saaq *SessionAppActivityQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range saaq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, saaq); err != nil {
				return err
			}
		}
	}
	for _, f := range saaq.ctx.Fields {
		if !sessionappactivity.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if saaq.path != nil {
		prev, err := saaq.path(ctx)
		if err != nil {
			return err
		}
		saaq.sql = prev
	}
	return nil
}

func (saaq *SessionAppActivityQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SessionAppActivity, error) {
	var (
		nodes       = []*SessionAppActivity{}
		_spec       = saaq.querySpec()
		loadedTypes = [2]bool{
			saaq.withSession != nil,
			saaq.withApplication != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SessionAppActivity).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SessionAppActivity{config: saaq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, saaq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := saaq.withSession; query != nil {
		if err := saaq.loadSession(ctx, query, nodes, nil,
			func(n *SessionAppActivity, e *Session) { n.Edges.Session = e }); err != nil {
			return nil, err
		}
	}
	if query := saaq.withApplication; query != nil {
		if err := saaq.loadApplication(ctx, query, nodes, nil,
			func(n *SessionAppActivity, e *Application) { n.Edges.Application = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (saaq *SessionAppActivityQuery) loadSession(ctx context.Context, query *SessionQuery, nodes []*SessionAppActivity, init func(*SessionAppActivity), assign func(*SessionAppActivity, *Session)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*SessionAppActivity)
	for i := range nodes {
		fk := nodes[i].SessionID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(session.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "session_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (saaq *SessionAppActivityQuery) loadApplication(ctx context.Context, query *ApplicationQuery, nodes []*SessionAppActivity, init func(*SessionAppActivity), assign func(*SessionAppActivity, *Application)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*SessionAppActivity)
	for i := range nodes {
		fk := nodes[i].ApplicationID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(application.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "application_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (saaq *SessionAppActivityQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := saaq.querySpec()
	_spec.Node.Columns = saaq.ctx.Fields
	if len(saaq.ctx.Fields) > 0 {
		_spec.Unique = saaq.ctx.Unique != nil && *saaq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, saaq.driver, _spec)
}

func (saaq *SessionAppActivityQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(sessionappactivity.Table, sessionappactivity.Columns, sqlgraph.NewFieldSpec(sessionappactivity.FieldID, field.TypeString))
	_spec.From = saaq.sql
	if unique := saaq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if saaq.path != nil {
		_spec.Unique = true
	}
	if fields := saaq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, sessionappactivity.FieldID)
		for i := range fields {
			if fields[i] != sessionappactivity.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if saaq.withSession != nil {
			_spec.Node.AddColumnOnce(sessionappactivity.FieldSessionID)
		}
		if saaq.withApplication != nil {
			_spec.Node.AddColumnOnce(sessionappactivity.FieldApplicationID)
		}
	}
	if ps := saaq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := saaq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := saaq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := saaq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (saaq *SessionAppActivityQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(saaq.driver.Dialect())
	t1 := builder.Table(sessionappactivity.Table)
	columns := saaq.ctx.Fields
	if len(columns) == 0 {
		columns = sessionappactivity.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if saaq.sql != nil {
		selector = saaq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if saaq.ctx.Unique != nil && *saaq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range saaq.predicates {
		p(selector)
	}
	for _, p := range saaq.order {
		p(selector)
	}
	if offset := saaq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := saaq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SessionAppActivityGroupBy is the group-by builder for SessionAppActivity entities.
type SessionAppActivityGroupBy struct {
	selector
	build *SessionAppActivityQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (saagb *SessionAppActivityGroupBy) Aggregate(fns ...AggregateFunc) *SessionAppActivityGroupBy {
	saagb.fns = append(saagb.fns, fns...)
	return saagb
}

// Scan applies the selector query and scans the result into the given value.
func (saagb *SessionAppActivityGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, saagb.build.ctx, ent.OpQueryGroupBy)
	if err := saagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SessionAppActivityQuery, *SessionAppActivityGroupBy](ctx, saagb.build, saagb, saagb.build.inters, v)
}

func (saagb *SessionAppActivityGroupBy) sqlScan(ctx context.Context, root *SessionAppActivityQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(saagb.fns))
	for _, fn := range saagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*saagb.flds)+len(saagb.fns))
		for _, f := range *saagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*saagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := saagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SessionAppActivitySelect is the builder for selecting fields of SessionAppActivity entities.
type SessionAppActivitySelect struct {
	*SessionAppActivityQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (saas *SessionAppActivitySelect) Aggregate(fns ...AggregateFunc) *SessionAppActivitySelect {
	saas.fns = append(saas.fns, fns...)
	return saas
}

// Scan applies the selector query and scans the result into the given value.
func (saas *SessionAppActivitySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, saas.ctx, ent.OpQuerySelect)
	if err := saas.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SessionAppActivityQuery, *SessionAppActivitySelect](ctx, saas.SessionAppActivityQuery, saas, saas.inters, v)
}

func (saas *SessionAppActivitySelect) sqlScan(ctx context.Context, root *SessionAppActivityQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(saas.fns))
	for _, fn := range saas.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*saas.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := saas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

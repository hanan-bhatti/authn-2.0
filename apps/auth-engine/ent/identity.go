// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/identity"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/user"
)

// Identity is the model entity for the Identity schema.
type Identity struct {
	config `json:"-"`
	// ID of the ent.
	// Unique Identity ID identifier (e.g. idn_1a2b3c)
	ID string `json:"id,omitempty"`
	// Owning User ID
	UserID string `json:"user_id,omitempty"`
	// Identity provider name (google, apple, github, twitter, facebook, discord)
	Provider string `json:"provider,omitempty"`
	// Unique subject/user ID issued by the external provider
	ProviderUserID string `json:"provider_user_id,omitempty"`
	// Email address returned by external social provider
	Email string `json:"email,omitempty"`
	// Avatar profile image URL returned by social provider
	AvatarURL string `json:"avatar_url,omitempty"`
	// Raw JSON profile claims returned by external provider
	ProfileData map[string]interface{} `json:"profile_data,omitempty"`
	// AES-256-GCM encrypted provider access token
	AccessTokenEncrypted string `json:"-"`
	// AES-256-GCM encrypted provider refresh token
	RefreshTokenEncrypted string `json:"-"`
	// Identity creation timestamp
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Identity updated timestamp
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the IdentityQuery when eager-loading is set.
	Edges        IdentityEdges `json:"edges"`
	selectValues sql.SelectValues
}

// IdentityEdges holds the relations/edges for other nodes in the graph.
type IdentityEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e IdentityEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Identity) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case identity.FieldProfileData:
			values[i] = new([]byte)
		case identity.FieldID, identity.FieldUserID, identity.FieldProvider, identity.FieldProviderUserID, identity.FieldEmail, identity.FieldAvatarURL, identity.FieldAccessTokenEncrypted, identity.FieldRefreshTokenEncrypted:
			values[i] = new(sql.NullString)
		case identity.FieldCreatedAt, identity.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Identity fields.
func (i *Identity) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for j := range columns {
		switch columns[j] {
		case identity.FieldID:
			if value, ok := values[j].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[j])
			} else if value.Valid {
				i.ID = value.String
			}
		case identity.FieldUserID:
			if value, ok := values[j].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[j])
			} else if value.Valid {
				i.UserID = value.String
			}
		case identity.FieldProvider:
			if value, ok := values[j].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field provider", values[j])
			} else if value.Valid {
				i.Provider = value.String
			}
		case identity.FieldProviderUserID:
			if value, ok := values[j].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field provider_user_id", values[j])
			} else if value.Valid {
				i.ProviderUserID = value.String
			}
		case identity.FieldEmail:
			if value, ok := values[j].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field email", values[j])
			} else if value.Valid {
				i.Email = value.String
			}
		case identity.FieldAvatarURL:
			if value, ok := values[j].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field avatar_url", values[j])
			} else if value.Valid {
				i.AvatarURL = value.String
			}
		case identity.FieldProfileData:
			if value, ok := values[j].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field profile_data", values[j])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &i.ProfileData); err != nil {
					return fmt.Errorf("unmarshal field profile_data: %w", err)
				}
			}
		case identity.FieldAccessTokenEncrypted:
			if value, ok := values[j].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field access_token_encrypted", values[j])
			} else if value.Valid {
				i.AccessTokenEncrypted = value.String
			}
		case identity.FieldRefreshTokenEncrypted:
			if value, ok := values[j].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field refresh_token_encrypted", values[j])
			} else if value.Valid {
				i.RefreshTokenEncrypted = value.String
			}
		case identity.FieldCreatedAt:
			if value, ok := values[j].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[j])
			} else if value.Valid {
				i.CreatedAt = value.Time
			}
		case identity.FieldUpdatedAt:
			if value, ok := values[j].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[j])
			} else if value.Valid {
				i.UpdatedAt = value.Time
			}
		default:
			i.selectValues.Set(columns[j], values[j])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Identity.
// This includes values selected through modifiers, order, etc.
func (i *Identity) Value(name string) (ent.Value, error) {
	return i.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the Identity entity.
func (i *Identity) QueryUser() *UserQuery {
	return NewIdentityClient(i.config).QueryUser(i)
}

// Update returns a builder for updating this Identity.
// Note that you need to call Identity.Unwrap() before calling this method if this Identity
// was returned from a transaction, and the transaction was committed or rolled back.
func (i *Identity) Update() *IdentityUpdateOne {
	return NewIdentityClient(i.config).UpdateOne(i)
}

// Unwrap unwraps the Identity entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (i *Identity) Unwrap() *Identity {
	_tx, ok := i.config.driver.(*txDriver)
	if !ok {
		panic("ent: Identity is not a transactional entity")
	}
	i.config.driver = _tx.drv
	return i
}

// String implements the fmt.Stringer.
func (i *Identity) String() string {
	var builder strings.Builder
	builder.WriteString("Identity(")
	builder.WriteString(fmt.Sprintf("id=%v, ", i.ID))
	builder.WriteString("user_id=")
	builder.WriteString(i.UserID)
	builder.WriteString(", ")
	builder.WriteString("provider=")
	builder.WriteString(i.Provider)
	builder.WriteString(", ")
	builder.WriteString("provider_user_id=")
	builder.WriteString(i.ProviderUserID)
	builder.WriteString(", ")
	builder.WriteString("email=")
	builder.WriteString(i.Email)
	builder.WriteString(", ")
	builder.WriteString("avatar_url=")
	builder.WriteString(i.AvatarURL)
	builder.WriteString(", ")
	builder.WriteString("profile_data=")
	builder.WriteString(fmt.Sprintf("%v", i.ProfileData))
	builder.WriteString(", ")
	builder.WriteString("access_token_encrypted=<sensitive>")
	builder.WriteString(", ")
	builder.WriteString("refresh_token_encrypted=<sensitive>")
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(i.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(i.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Identities is a parsable slice of Identity.
type Identities []*Identity

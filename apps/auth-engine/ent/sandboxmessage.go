// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/sandboxmessage"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/tenant"
)

// SandboxMessage is the model entity for the SandboxMessage schema.
type SandboxMessage struct {
	config `json:"-"`
	// ID of the ent.
	// Unique Sandbox Message ID identifier (e.g. sbxmsg_1a2b3c)
	ID string `json:"id,omitempty"`
	// Owning Tenant ID
	TenantID string `json:"tenant_id,omitempty"`
	// Environment the message was captured in. Only test rows are ever written; the field exists so this entity is narrowed by the same predicate as every other environment-scoped one, which means a live-key query cannot read sandbox traffic even if a row were somehow written as live.
	Environment sandboxmessage.Environment `json:"environment,omitempty"`
	// Which transport would have carried the message
	Channel sandboxmessage.Channel `json:"channel,omitempty"`
	// Address the message was addressed to: an email address, or an E.164 phone number for SMS
	Recipient string `json:"recipient,omitempty"`
	// Rendered subject line. Empty for SMS, which has none.
	Subject string `json:"subject,omitempty"`
	// Fully rendered message body, exactly as the provider would have received it
	Body string `json:"body,omitempty"`
	// Identifier of the template that produced the body (e.g. email_verification), for a harness that wants to assert on which message was triggered rather than parse the body
	Template string `json:"template,omitempty"`
	// The one-time code carried by the message, lifted out at capture time. Present so an automated test can complete a verification flow without parsing rendered HTML; empty for messages that carry no code.
	Code string `json:"code,omitempty"`
	// Template variables and any structured values the sender attached, such as an action link
	Metadata map[string]interface{} `json:"metadata,omitempty"`
	// When the message was captured
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SandboxMessageQuery when eager-loading is set.
	Edges        SandboxMessageEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SandboxMessageEdges holds the relations/edges for other nodes in the graph.
type SandboxMessageEdges struct {
	// Tenant holds the value of the tenant edge.
	Tenant *Tenant `json:"tenant,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// TenantOrErr returns the Tenant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SandboxMessageEdges) TenantOrErr() (*Tenant, error) {
	if e.Tenant != nil {
		return e.Tenant, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: tenant.Label}
	}
	return nil, &NotLoadedError{edge: "tenant"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SandboxMessage) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case sandboxmessage.FieldMetadata:
			values[i] = new([]byte)
		case sandboxmessage.FieldID, sandboxmessage.FieldTenantID, sandboxmessage.FieldEnvironment, sandboxmessage.FieldChannel, sandboxmessage.FieldRecipient, sandboxmessage.FieldSubject, sandboxmessage.FieldBody, sandboxmessage.FieldTemplate, sandboxmessage.FieldCode:
			values[i] = new(sql.NullString)
		case sandboxmessage.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SandboxMessage fields.
func (sm *SandboxMessage) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case sandboxmessage.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				sm.ID = value.String
			}
		case sandboxmessage.FieldTenantID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value.Valid {
				sm.TenantID = value.String
			}
		case sandboxmessage.FieldEnvironment:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field environment", values[i])
			} else if value.Valid {
				sm.Environment = sandboxmessage.Environment(value.String)
			}
		case sandboxmessage.FieldChannel:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field channel", values[i])
			} else if value.Valid {
				sm.Channel = sandboxmessage.Channel(value.String)
			}
		case sandboxmessage.FieldRecipient:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field recipient", values[i])
			} else if value.Valid {
				sm.Recipient = value.String
			}
		case sandboxmessage.FieldSubject:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field subject", values[i])
			} else if value.Valid {
				sm.Subject = value.String
			}
		case sandboxmessage.FieldBody:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field body", values[i])
			} else if value.Valid {
				sm.Body = value.String
			}
		case sandboxmessage.FieldTemplate:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field template", values[i])
			} else if value.Valid {
				sm.Template = value.String
			}
		case sandboxmessage.FieldCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field code", values[i])
			} else if value.Valid {
				sm.Code = value.String
			}
		case sandboxmessage.FieldMetadata:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field metadata", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &sm.Metadata); err != nil {
					return fmt.Errorf("unmarshal field metadata: %w", err)
				}
			}
		case sandboxmessage.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				sm.CreatedAt = value.Time
			}
		default:
			sm.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SandboxMessage.
// This includes values selected through modifiers, order, etc.
func (sm *SandboxMessage) Value(name string) (ent.Value, error) {
	return sm.selectValues.Get(name)
}

// QueryTenant queries the "tenant" edge of the SandboxMessage entity.
func (sm *SandboxMessage) QueryTenant() *TenantQuery {
	return NewSandboxMessageClient(sm.config).QueryTenant(sm)
}

// Update returns a builder for updating this SandboxMessage.
// Note that you need to call SandboxMessage.Unwrap() before calling this method if this SandboxMessage
// was returned from a transaction, and the transaction was committed or rolled back.
func (sm *SandboxMessage) Update() *SandboxMessageUpdateOne {
	return NewSandboxMessageClient(sm.config).UpdateOne(sm)
}

// Unwrap unwraps the SandboxMessage entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sm *SandboxMessage) Unwrap() *SandboxMessage {
	_tx, ok := sm.config.driver.(*txDriver)
	if !ok {
		panic("ent: SandboxMessage is not a transactional entity")
	}
	sm.config.driver = _tx.drv
	return sm
}

// String implements the fmt.Stringer.
func (sm *SandboxMessage) String() string {
	var builder strings.Builder
	builder.WriteString("SandboxMessage(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sm.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(sm.TenantID)
	builder.WriteString(", ")
	builder.WriteString("environment=")
	builder.WriteString(fmt.Sprintf("%v", sm.Environment))
	builder.WriteString(", ")
	builder.WriteString("channel=")
	builder.WriteString(fmt.Sprintf("%v", sm.Channel))
	builder.WriteString(", ")
	builder.WriteString("recipient=")
	builder.WriteString(sm.Recipient)
	builder.WriteString(", ")
	builder.WriteString("subject=")
	builder.WriteString(sm.Subject)
	builder.WriteString(", ")
	builder.WriteString("body=")
	builder.WriteString(sm.Body)
	builder.WriteString(", ")
	builder.WriteString("template=")
	builder.WriteString(sm.Template)
	builder.WriteString(", ")
	builder.WriteString("code=")
	builder.WriteString(sm.Code)
	builder.WriteString(", ")
	builder.WriteString("metadata=")
	builder.WriteString(fmt.Sprintf("%v", sm.Metadata))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(sm.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SandboxMessages is a parsable slice of SandboxMessage.
type SandboxMessages []*SandboxMessage

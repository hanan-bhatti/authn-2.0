// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/tenant"
	"github.com/hanan-bhatti/authn-2.0/apps/auth-engine/ent/tenantenvironment"
)

// TenantEnvironment is the model entity for the TenantEnvironment schema.
type TenantEnvironment struct {
	config `json:"-"`
	// ID of the ent.
	// Unique Tenant Environment ID identifier (e.g. tenv_1a2b3c)
	ID string `json:"id,omitempty"`
	// Owning Tenant ID
	TenantID string `json:"tenant_id,omitempty"`
	// Which environment these settings govern. Immutable: the pair (tenant_id, environment) identifies the row, and repointing it would move a whole configuration between environments in one write. There is deliberately no default, because a settings row that does not say which environment it configures is not a meaningful row.
	Environment tenantenvironment.Environment `json:"environment,omitempty"`
	// JSON blob containing brand colors, logo URL, custom CSS, and email templates
	BrandingConfig map[string]interface{} `json:"branding_config,omitempty"`
	// JSON blob containing password complexity policy rules
	PasswordPolicy map[string]interface{} `json:"password_policy,omitempty"`
	// JSON blob containing tenant security policy settings (require_email_verification, email_verification_mode), including the nested impersonation_policy key
	SecurityPolicy map[string]interface{} `json:"security_policy,omitempty"`
	// JSON blob containing tenant account recovery policy rules
	RecoveryPolicy map[string]interface{} `json:"recovery_policy,omitempty"`
	// Per-provider OAuth2 configuration keyed by provider name (google, github, discord, etc.). Each entry holds: enabled bool, client_id string, client_secret_encrypted string (AES-256-GCM). Client secrets are never returned in API responses. Held per environment so a sandbox can point at a provider's test credentials while live points at the real ones.
	SocialProviders map[string]interface{} `json:"social_providers,omitempty"`
	// JSON blob containing tenant role & permission restrictions and assignment policies
	RolePolicy map[string]interface{} `json:"role_policy,omitempty"`
	// JSON blob containing cookie SameSite mode and session/access token lifetimes. Lives here rather than in the deployment environment because a customer changes it and it must take effect without a redeploy; the cookie Domain stays an env value because a server can only set cookies for a domain it is served from.
	SessionPolicy map[string]interface{} `json:"session_policy,omitempty"`
	// When this row last received a published copy of another environment's settings. Nil on a row that has only ever been edited directly.
	PublishedAt *time.Time `json:"published_at,omitempty"`
	// Timestamp when the settings row was created
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Timestamp when the settings row was last updated
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TenantEnvironmentQuery when eager-loading is set.
	Edges        TenantEnvironmentEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TenantEnvironmentEdges holds the relations/edges for other nodes in the graph.
type TenantEnvironmentEdges struct {
	// Tenant holds the value of the tenant edge.
	Tenant *Tenant `json:"tenant,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// TenantOrErr returns the Tenant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TenantEnvironmentEdges) TenantOrErr() (*Tenant, error) {
	if e.Tenant != nil {
		return e.Tenant, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: tenant.Label}
	}
	return nil, &NotLoadedError{edge: "tenant"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TenantEnvironment) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case tenantenvironment.FieldBrandingConfig, tenantenvironment.FieldPasswordPolicy, tenantenvironment.FieldSecurityPolicy, tenantenvironment.FieldRecoveryPolicy, tenantenvironment.FieldSocialProviders, tenantenvironment.FieldRolePolicy, tenantenvironment.FieldSessionPolicy:
			values[i] = new([]byte)
		case tenantenvironment.FieldID, tenantenvironment.FieldTenantID, tenantenvironment.FieldEnvironment:
			values[i] = new(sql.NullString)
		case tenantenvironment.FieldPublishedAt, tenantenvironment.FieldCreatedAt, tenantenvironment.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TenantEnvironment fields.
func (te *TenantEnvironment) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case tenantenvironment.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				te.ID = value.String
			}
		case tenantenvironment.FieldTenantID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value.Valid {
				te.TenantID = value.String
			}
		case tenantenvironment.FieldEnvironment:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field environment", values[i])
			} else if value.Valid {
				te.Environment = tenantenvironment.Environment(value.String)
			}
		case tenantenvironment.FieldBrandingConfig:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field branding_config", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &te.BrandingConfig); err != nil {
					return fmt.Errorf("unmarshal field branding_config: %w", err)
				}
			}
		case tenantenvironment.FieldPasswordPolicy:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field password_policy", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &te.PasswordPolicy); err != nil {
					return fmt.Errorf("unmarshal field password_policy: %w", err)
				}
			}
		case tenantenvironment.FieldSecurityPolicy:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field security_policy", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &te.SecurityPolicy); err != nil {
					return fmt.Errorf("unmarshal field security_policy: %w", err)
				}
			}
		case tenantenvironment.FieldRecoveryPolicy:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field recovery_policy", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &te.RecoveryPolicy); err != nil {
					return fmt.Errorf("unmarshal field recovery_policy: %w", err)
				}
			}
		case tenantenvironment.FieldSocialProviders:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field social_providers", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &te.SocialProviders); err != nil {
					return fmt.Errorf("unmarshal field social_providers: %w", err)
				}
			}
		case tenantenvironment.FieldRolePolicy:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field role_policy", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &te.RolePolicy); err != nil {
					return fmt.Errorf("unmarshal field role_policy: %w", err)
				}
			}
		case tenantenvironment.FieldSessionPolicy:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field session_policy", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &te.SessionPolicy); err != nil {
					return fmt.Errorf("unmarshal field session_policy: %w", err)
				}
			}
		case tenantenvironment.FieldPublishedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field published_at", values[i])
			} else if value.Valid {
				te.PublishedAt = new(time.Time)
				*te.PublishedAt = value.Time
			}
		case tenantenvironment.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				te.CreatedAt = value.Time
			}
		case tenantenvironment.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				te.UpdatedAt = value.Time
			}
		default:
			te.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TenantEnvironment.
// This includes values selected through modifiers, order, etc.
func (te *TenantEnvironment) Value(name string) (ent.Value, error) {
	return te.selectValues.Get(name)
}

// QueryTenant queries the "tenant" edge of the TenantEnvironment entity.
func (te *TenantEnvironment) QueryTenant() *TenantQuery {
	return NewTenantEnvironmentClient(te.config).QueryTenant(te)
}

// Update returns a builder for updating this TenantEnvironment.
// Note that you need to call TenantEnvironment.Unwrap() before calling this method if this TenantEnvironment
// was returned from a transaction, and the transaction was committed or rolled back.
func (te *TenantEnvironment) Update() *TenantEnvironmentUpdateOne {
	return NewTenantEnvironmentClient(te.config).UpdateOne(te)
}

// Unwrap unwraps the TenantEnvironment entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (te *TenantEnvironment) Unwrap() *TenantEnvironment {
	_tx, ok := te.config.driver.(*txDriver)
	if !ok {
		panic("ent: TenantEnvironment is not a transactional entity")
	}
	te.config.driver = _tx.drv
	return te
}

// String implements the fmt.Stringer.
func (te *TenantEnvironment) String() string {
	var builder strings.Builder
	builder.WriteString("TenantEnvironment(")
	builder.WriteString(fmt.Sprintf("id=%v, ", te.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(te.TenantID)
	builder.WriteString(", ")
	builder.WriteString("environment=")
	builder.WriteString(fmt.Sprintf("%v", te.Environment))
	builder.WriteString(", ")
	builder.WriteString("branding_config=")
	builder.WriteString(fmt.Sprintf("%v", te.BrandingConfig))
	builder.WriteString(", ")
	builder.WriteString("password_policy=")
	builder.WriteString(fmt.Sprintf("%v", te.PasswordPolicy))
	builder.WriteString(", ")
	builder.WriteString("security_policy=")
	builder.WriteString(fmt.Sprintf("%v", te.SecurityPolicy))
	builder.WriteString(", ")
	builder.WriteString("recovery_policy=")
	builder.WriteString(fmt.Sprintf("%v", te.RecoveryPolicy))
	builder.WriteString(", ")
	builder.WriteString("social_providers=")
	builder.WriteString(fmt.Sprintf("%v", te.SocialProviders))
	builder.WriteString(", ")
	builder.WriteString("role_policy=")
	builder.WriteString(fmt.Sprintf("%v", te.RolePolicy))
	builder.WriteString(", ")
	builder.WriteString("session_policy=")
	builder.WriteString(fmt.Sprintf("%v", te.SessionPolicy))
	builder.WriteString(", ")
	if v := te.PublishedAt; v != nil {
		builder.WriteString("published_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(te.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(te.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// TenantEnvironments is a parsable slice of TenantEnvironment.
type TenantEnvironments []*TenantEnvironment
